# go-trader Project Context

## Environment
- Go 1.26.2 — install via your platform's package manager (e.g. `brew install go@1.26` on macOS). If `go` is not in PATH, use the full binary path (e.g. `/opt/homebrew/bin/go` on Apple Silicon).
- Python venv at `.venv/bin/python3` (used by executor.go at runtime); deps via `uv` (`pyproject.toml` / `uv.lock`).
- In git worktrees, `.venv` is NOT copied — use the main repo's venv: `<main-repo>/.venv/bin/python3`.

## Quick Flow
- **New server:** tell your agent `install https://github.com/richkuo/go-trader and init`.

## Setup
- `uv sync` — install Python deps into `.venv`
- Copy `scheduler/config.example.json` → `scheduler/config.json` and fill in API keys

## Repo Structure
- `scheduler/` — Go scheduler (single `package main`); all .go files compile together. Key files:
  - `executor.go` — Python subprocess runner; `pythonSemaphore` caps concurrency at 4, `scriptTimeout=30s`, SIGKILLs process group on timeout.
  - `server.go` — `/status`, `/health`, `/history`; `DefaultStatusPort=8099`, auto-fallback up to 5 ports; precedence: `--status-port` > `cfg.StatusPort` > default.
  - `discord.go` — `discordgo.Session` wrapper; `SendMessage`/`SendDM`/`AskDM`; `FormatCategorySummary`. Category table strategy column (#514): `catTableStrategyWidth=18`; `summaryStrategyLabel` substitutes aliases (`tiered-atr`→`tatr`, `tiered-pct`→`tpct`) then truncates IDs to fit — header cols `Init | Value | PnL | PnL% | DD | Wallet% | Tf | Int | #T | W/L` (DD rendered `%.0f%%`); `Book Sharpe` footer. Open-position lines (`collectPositions(sc, ss, prices)`) append `SL: $<px> (<signed_pct>%)` whenever `Position.StopLossTriggerPx > 0` (no longer gated on `StopLossOID`, #529); for strategies whose `close_strategies` include `tiered_tp_atr` or `tiered_tp_atr_live`, append `TP1: $<1×ATR> | TP2: $<2×ATR>` priced from `Position.AvgCost` ± `EntryATR` (long add, short subtract; `strategyUsesTieredTPATRClose`, #528/#529); `<N>x ($<margin> margin)` when `Leverage > 1` (#485). Trade DMs use `isTradeCloseDetails(trade.Details)` (case-insensitive `"close"` substring) so partial-close legs render as `TRADE CLOSED` with PnL styling (#530); `telegram.FormatTradeDMPlain` shares the helper. `FormatTradeDM` / `FormatTradeDMPlain` inject `| Regime: X` before `| Mode:` when `Trade.Regime` is non-empty (#542).
  - `init.go` — `go-trader init` wizard + `--json` mode; `generateConfig(InitOptions)` is pure/testable. Holds `bidirectionalPerpsStrategies`, `knownShortNames`, `defaultSpotStrategies` / `defaultPerpsStrategies` / `defaultFuturesStrategies`.
  - `config.go` / `config_migration.go` — `Config`, `StrategyConfig`; `LoadConfig` infers `Platform` from ID prefix; `CurrentConfigVersion=11`; `RiskFreeRate *float64` (nil → `DefaultAnnualRiskFreeRate`); perps `Leverage` is exchange/risk leverage and `SizingLeverage` controls notional order size (omitted → `Leverage`); `MarginPerTradeUSD` (`*float64`, perps-only opt-in #518) overrides `SizingLeverage` for opens — `notional = min(MarginPerTradeUSD, cash) × exchange_leverage` via `PerpsOpenNotional` / `ComputePerpsOpenNotional`; the legacy `× 0.95` safety buffer was removed in #518 (operators wanting headroom set a smaller `sizing_leverage` / `margin_per_trade_usd` explicitly); `StopLossPct` / `StopLossMarginPct` / `TrailingStopPct` / `TrailingStopATRMult` / `StopLossATRMult` are HL-perps-only `*float64` (`MaxDrawdownPct` auto-stop applies only when no positive SL among the five; explicit 0 → opt-out where defined; mutually exclusive when positive — see `EffectiveStopLossPct`); `LoadConfig` defaults sole-owner HL perps strategies with all five omitted to `StopLossATRMult=DefaultStopLossATRMult` (1.0× ATR) so every position has volatility-adjusted exchange-side protection out of the box (#562); `TrailingStopMinMovePct` (`%`/ATR-mult trailing debounce, default 0.5%); `MarginMode` (HL perps only, `isolated`|`cross`, default `isolated`); `RegimeConfig{Enabled bool, Period int, ADXThreshold float64}` — global regime detector; `Config.Regime *RegimeConfig` (default disabled; defaults `Period=14`, `ADXThreshold=20` injected only when `Enabled=true`); `StrategyConfig.AllowedRegimes []string` — per-strategy entry gate (empty = allow all; `ValidateConfig` rejects unknown labels and rejects `AllowedRegimes` on type=options; warns when `Regime.Enabled=false` and any strategy has `AllowedRegimes` set, #541/#553/#558).
  - `hyperliquid_trailing_stop.go` — synthetic `%` trailing (#501) and ATR-mult distance (#507 on `TrailingStopATRMult` + `Position.EntryATR`): `computeTrailingStopUpdate` walks high-water mark by side and emits cancel/replace when the trigger moves favorably by `TrailingStopMinMovePct`; `runHyperliquidTrailingStopUpdate` invokes `RunHyperliquidUpdateStopLoss` and warns on cancel failure (race against HL 1000-trigger cap).
  - `state.go` / `state_presence.go` / `db.go` — SQLite-only state (`modernc.org/sqlite`); `OpenStateDB`, `SaveStateWithDB`, `LoadStateWithDB`. Tables: `app_state`, `strategies`, `positions`, `closed_positions`, `option_positions`, `closed_option_positions`, `trades`, `portfolio_risk`, `kill_switch_events`, `correlation_snapshot`. `app_state.last_summary_post` (#474) stores wall-clock summary cadence per channel; `trades.position_id` / `positions.position_id` / `option_positions.position_id` (#471) group trade legs into round trips via `idx_trades_strategy_position`. `InsertTrade` writes immediately. `CheckStatePresence` warns on missing DB (`GO_TRADER_ALLOW_MISSING_STATE=1` for first-run). `trades.regime TEXT NOT NULL DEFAULT ''` column (#542) stores the regime label at trade time; idempotent migration via duplicate-column guard.
  - `risk.go` / `strategy_interval.go` — `CheckRisk` takes `*PlatformRiskAssist`; `perpsMarginDrawdownInputs` for leverage-aware perps DD. `effectiveStrategyIntervalSeconds` accelerates checks in DD warn band; `WarningSent` repeats warnings every cycle while in band.
  - `kill_switch_close.go` + per-platform closers (`hyperliquid_balance.go`, `okx_close.go`, `robinhood_close.go`, `topstep_close.go`, `robinhood_pending_close.go`, `operator_required_close.go`) — portfolio kill switch + per-strategy CB drains.
  - `*_marks.go`, `deribit.go` — native Go price fetchers (HL `allMids`, OKX swap tickers, Deribit ticker); base URL exposed as `var xxxMainnetURL` for httptest stubs.
  - `regime.go` — `regimeBlocksOpen(allowed, current, posQty)` — blocks new entries when `current` ∉ `allowed` and `posQty==0`; close legs (any existing position quantity) always pass through; `regimeAllowsEntry` is the pure inner helper. `StrategyState.Regime string` persisted in state; `StratStatus.Regime` exposed in `/status` JSON.
  - `sharpe.go`, `failure_alerts.go`, `correlation.go`, `leaderboard.go`, `notifier.go`, `telegram.go`, `updater.go`, `pricer.go` (+ `ibkr_pricer.go`), `shared_wallet.go`, `version.go`, `prompt.go`, `balance.go`, `portfolio.go`, `options.go`, `logger.go`, `tradingview_export.go`, `config_reload.go`.
- `shared_scripts/` — Python entry-points: `check_strategy.py` (spot), `check_options.py` (`--platform=deribit|ibkr|robinhood|okx`), `check_price.py`, `check_hyperliquid.py`, `check_topstep.py`, `check_robinhood.py`, `check_okx.py` (`--inst-type=spot|swap`), `check_balance.py`, `fetch_futures_marks.py`, plus per-platform `close_*.py` / `fetch_*_balance.py` / `fetch_*_positions.py`.
- `platforms/<name>/adapter.py` — one `*ExchangeAdapter` class per file: `deribit`, `ibkr`, `binanceus`, `hyperliquid`, `topstep`, `robinhood`, `okx`, `luno`.
- `shared_tools/` — `pricing.py`, `exchange_base.py`, `data_fetcher.py`, `storage.py`, `htf_filter.py`, `regime.py` (`compute_regime()`, `latest_regime()`, `ensure_regime_columns()` — 3-state ADX+DI labels: `trending_up` / `trending_down` / `ranging`; default period=14, threshold=20; underlying ADX math shared with `shared_strategies/open/adx_trend.py` via `_compute_adx_components()`).
- `shared_strategies/` — open strategy source of truth lives in `open/registry.py` (`@register_strategy`, `build_registry(platform)`, `PLATFORM_ORDER`, #511). Cross-platform open modules live under `open/`, with `open/spot/strategies.py` and `open/futures/strategies.py` as thin shims — **do not edit shims to add strategies**. Options still use `options/strategies.py`; close evaluators register in `close/registry.py` (#511 split from open registry).
- `backtest/` — `backtester.py`, `optimizer.py`, `reporter.py`, `registry_loader.py`, `run_backtest.py`, `backtest_options.py`, `backtest_theta.py`, `tests/`.
- `SKILL.md`, `AGENTS.md` — agent operations guides; `scripts/install-service.sh` — systemd installer; `.github/workflows/` — CI, release, Codex/Claude bots.

## Key Patterns
- Run git from repo root (not `scheduler/`). Prefer `go -C scheduler build .` over `cd scheduler && ...` to avoid cwd drift.
- **New platform:** (1) `platforms/<name>/adapter.py` + `__init__.py`, (2) `shared_scripts/check_<name>.py`, (3) `executor.go`, (4) `config.go` (prefix + validation), (5) `fees.go`, (6) `main.go` (dispatch), (7) `init.go` (wizard + generateConfig), (8) `pyproject.toml`.
- **New options-on-existing-platform:** extend adapter with `get_vol_metrics`, `get_real_expiry`, `get_real_strike`, `get_premium_and_greeks`; add to `check_options.py` + `CalculateOptionFee` + init `OptionPlatforms`.
- Adapters loaded via `importlib`; class detected by `endswith("ExchangeAdapter")` (one per file). Check scripts must use only public adapter methods.
- **Close registry import:** never `import registry` directly to reach `shared_strategies/close/registry.py` — name collides with the open registry. Use `from close_registry_loader import evaluate, list_strategies, build_close_registry` (in `shared_tools/`); it loads via `importlib.util.spec_from_file_location` and caches.
- Subprocess contract: scripts always emit JSON to stdout (even on error); exit 1 on error; Go parses regardless of exit code.
- **State locking:** `mu sync.RWMutex` guards `state`. Per-strategy loop has 6 phases: RLock(read inputs) → Lock(CheckRisk) → no lock(subprocess) → Lock(execute signal) → RLock/no lock/Lock(marks) → RLock(status). Audit: `grep -n "mu\.\(R\)\?Lock\(\)\|mu\.\(R\)\?Unlock\(\)" scheduler/main.go`.
- Platform dispatch: use `s.Platform == "ibkr"`, never ID prefix. ID → platform map: `hl-` HL, `ibkr-` IBKR, `deribit-` Deribit, `ts-` TopStep, `rh-` Robinhood, `okx-` OKX, `luno-` Luno, else BinanceUS.
- Strategy types: `spot`, `options`, `perps`, `futures`. Perps paper reuses `ExecuteSpotSignal`; live calls `RunHyperliquidExecute` first. Futures use `ExecuteFuturesSignal`.
- Open/close split (#480/#483/#496): `StrategyConfig.OpenStrategy` overrides the entry strategy name from `Args[0]`; `CloseStrategies` is an ordered list of exit evaluators, with max `close_fraction` winning. When `CloseStrategies` is empty, `effective_close_strategies` falls back to the open strategy as the implicit close. Composition lives in `scheduler/strategy_composition.go` (Go) and `shared_tools/strategy_composition.py` (Python). Old configs omit these fields and stay on the exact legacy subprocess path. Opted-in spot/perps/futures check scripts emit `open_action`, `close_fraction`, `close_strategy`, and the composed legacy `signal`; close composition runs before opening. Options strategies do not use this split yet. `StrategyDecisionFields` in `strategy_composition.go` includes `Regime string` — propagated to all 5 result structs (SpotResult, HyperliquidResult, OKXResult, RobinhoodResult, TopStepResult) and written to `stratState.Regime` each cycle (#541). **Partial-close execution (#519/#521):** `close_fraction` from the registry is now honored by all live and paper paths — `Execute*Signal*` funcs accept `closeFraction float64`; partial close decrements `pos.Quantity` without deleting the position, preserves `InitialQuantity`, and skips the open-leg fall-through. `perpsLiveOrderSize` scales close qty by fraction; TopStep rounds down to whole contracts with a sub-contract no-op guard. Legacy wrappers pass `0` so kill-switch/stop-loss/forceClose paths keep full-close semantics. SL cancel/replace is suppressed on partial close (trigger continues to protect residual). **Position-aware close evaluators (#496/#500):** check scripts forward `--position-side`, `--position-avg-cost`, `--position-qty`, `--position-initial-qty`, `--position-entry-atr` (Go: `PositionCtx`; non-zero floats only). Close functions opt in by accepting `side`/`avg_cost`/`current_quantity`/`initial_quantity`/`entry_atr` kwargs (or `**params`) — `strip_unsupported_position_context` filters those keys for legacy close functions, and the cache key includes the merged params so runs with vs. without context don't alias. Reference impl: `tp_at_pct` in registry.
- **Live exec guard:** every dispatch in main.go must use `liveExecFailed`; when `runXxxExecuteOrder` returns `ok2=false`, set true and skip state update.
- **Skip-reason guards:** live order helpers (`runHyperliquidExecuteOrder`, `runOKXExecuteOrder`, `runRobinhoodExecuteOrder`, `runTopStepExecuteOrder`) must check the same conditions as `ExecuteXxxSignal` BEFORE spawning the executor — otherwise on-chain fills land with no Trade record. Use `PerpsOrderSkipReason` / `SpotOrderSkipReason` / `FuturesOrderSkipReason`. Capture `posSide` alongside `posQty` in Phase 1 RLock — `Position.Quantity` is always positive.
- **Bidirectional perps:** `StrategyConfig.AllowShorts` per-strategy opt-in. Strategies emitting `signal=-1` as short entries (`triple_ema_bidir`) must be in `bidirectionalPerpsStrategies` in init.go. `ExecutePerpsSignal(..., allowShorts, ...)` and `PerpsOrderSkipReason(signal, posSide, allowShorts)` must thread `sc.AllowShorts`. Live flip sizing: `perpsLiveOrderSize`; catastrophic flip degrades to close-only. Threads `pos.AvgCost` from Phase 1 snapshot.
- `dueStrategies` is value-copied from `cfg.Strategies` — mutations don't persist. Update `cfg.Strategies` first.
- Notification features: add methods to `MultiNotifier`, don't access `backends` directly.
- Hyperliquid sys.path conflict: SDK package `hyperliquid` clashes with `platforms/hyperliquid/` — add `platforms/hyperliquid/` directly to sys.path, then `from adapter import HyperliquidExchangeAdapter`.
- HL funding rates: `info.meta_and_asset_ctxs()` (NOT `info.meta()`); response uses parallel arrays.
- Fees: `CalculatePlatformSpotFee(platform, value)` (HL 0.035%, RH 0%, BinanceUS 0.1%); `CalculatePlatformFuturesFee(sc, contracts)`.
- Position ownership: `Position.OwnerStrategyID`; `syncHyperliquidAccountPositions` reconciles with owner only.
- **State is SQLite-only** (`scheduler/state.db`, `cfg.DBFile`). Legacy JSON paths removed. Trades persist immediately via `RecordTrade` → `StateDB.InsertTrade`. `ClosedPositions` flushed atomically by `SaveState`. Leaderboard built on-demand (`BuildLeaderboardMessages`).
- **Trades schema (close legs):** `trades.is_close INTEGER` / `realized_pnl REAL` flag close legs at insert time; `trades.regime TEXT NOT NULL DEFAULT ''` stores the market regime label stamped from `stratState.Regime` before every `RecordTrade` call (#542). `LifetimeTradeStatsAll(db)` runs one COUNT/SUM per cycle for `#T` / `W/L` columns; wins use strict `realized_pnl > 0` so breakeven closes don't inflate W. Round-trips group close legs by `(strategy_id, position_id)` so partial-close legs collapse into one trade (#471); legacy NULL/empty `position_id` rows still count as one round-trip each. `FormatCategorySummary` renders zero when a strategy is absent from the lifetime map — SQLite is authoritative, no in-memory fallback (#472). `backfillTradeCloseFlags` parses legacy `Details` strings (`PnL: $X` / `PnL=$X`); idempotent via `details LIKE '%PnL%'` guard.
- **Live execute fills:** HL/OKX/RH/TopStep `--execute` scripts emit `fillFee` (and per-leg `fillFees`) plus exchange order IDs (`fillOID` / `exchange_order_id`); Go threads these into `Trade.Fees` and `Trade.ExchangeOrderID` BEFORE `RecordTrade`. Python truthy filter (`if oid:`) — never `is not None` — so empty strings and numeric 0 don't land. TopStep precedence: prefer `orderId` even when numeric 0, fall back to `id` only when `orderId` is missing. Tests live in `shared_scripts/test_check_execute_fees.py`.
- **HL kill-switch fill accounting:** `forceCloseKillSwitchPositions(s, sc, prices, hlFills, hlLiveAll, hlVirtualQty, logger)` records real on-chain close fills, not pre-trade estimates. Shared-coin fills are split by **virtual quantity at snapshot time** (not capital weight): `main.go` calls `snapshotHyperliquidVirtualQuantities(state.Strategies, hlLiveAll)` under the RLock BEFORE the close mutates state, and threads the snapshot through `forceCloseKillSwitchPositions` → `applyHyperliquidKillSwitchCloseFill` → `hyperliquidKillSwitchFillShare(sc, coin, fillSz, fillFee, hlLiveAll, virtualQty)`. `hyperliquidKillSwitchFillShare` fails closed (returns `0, 0`) when `sc.ID` isn't among peers or has no virtual qty — never let a misconfigured caller claim the entire portfolio fill.
- **Trade.Side on close legs:** close trades record buy/sell semantics, not the literal string `"close"`. Use `closeTradeSide(positionSide)` (perps/spot — `"short"` → `"buy"`, else `"sell"`) and `optionCloseTradeSide(action)` (options — `"sell"` → `"buy"`, else `"sell"`). Applies in `forceCloseAllPositions`, `recordPerpsStopLossClose`, `executeOptionClose`, and `CheckThetaHarvest`. Default-to-`"sell"` preserves legacy behavior for missing/unknown sides.
- **TradingView export:** if the user says "export data to TradingView", first ask which strategy IDs to export or whether to export all strategies. Use `./go-trader export tradingview --strategy <id> --output <file>` for one or more selected strategies, or `./go-trader export tradingview --all --output <file>`. Exports are sourced from SQLite trades and may require `tradingview_export.symbol_overrides` for platforms/symbols without a safe built-in TradingView mapping.
- **Map iteration:** ALWAYS `sort.Strings(keys)` before formatting any operator-facing or test-asserted output. Go map iteration is randomized.
- `cfg.Discord.Channels` / `Telegram.Channels` / `DMChannels` are `map[string]string`; keys: `spot`, `options`, `<platform>`, `<platform>-paper`. `cfg.Discord.OwnerID` from `DISCORD_OWNER_ID` env var (priority over config).
- `cfg.SummaryFrequency map[string]string`: Go duration / alias (`hourly`, `daily`, `every`/`per_check`/`always`) / empty (legacy). `ShouldPostSummary(freq, continuous, hasTrades, lastPost, now)` — wall-clock throttling (#474), not cycle-count; `hasTrades=true` always forces a post; per-channel `lastPost` tracked in `lastSummaryPost map[string]time.Time` (single-writer in main loop, mirrored into `state.LastSummaryPost` during save) and persisted in `app_state.last_summary_post` so cadence survives restart/auto-update. Use `cloneTimeMap` when copying between the local map and `AppState`.
- `cfg.Correlation` (`Enabled=false`, `MaxConcentrationPct=60`, `MaxSameDirectionPct=75`); `cfg.AutoUpdate`: `off|daily|heartbeat`.
- **Regime detection (#539/#540/#541/#546/#548/#549/#551/#553/#558):** `shared_tools/regime.py` emits 3-state labels (`trending_up`, `trending_down`, `ranging`) via ADX+DI; canonical label set defined in `validRegimeLabels` in `config.go`. All 5 standard check scripts (`check_strategy.py`, `check_hyperliquid.py`, `check_okx.py`, `check_robinhood.py`, `check_topstep.py`) call `latest_regime(df)` and inject the label into strategy params; `check_options.py` emits `"regime": null` (deferred). `"regime"` is in `POSITION_CONTEXT_PARAM_KEYS` so `strip_unsupported_position_context` drops it for strategies that don't accept a `regime=` kwarg. Go executor appends `--regime-enabled`, `--regime-period`, `--regime-adx-threshold` to all 5 check-script calls via `appendRegimeArgs` (#558) — if `--regime-enabled` is absent, check scripts skip detection and emit `regime=""`. Entry gate is `regimeBlocksOpen(allowed, current, posQty)` at 6 dispatch sites in main.go; close legs (posQty>0) always pass through regardless of regime. `AllowedRegimes` is SIGHUP-compatible (hot-reload); `cfg.Regime` block changes require restart (mirrors `Correlation`).
- **Strategy registries (#511):** open implementations live once under `shared_strategies/open/`, source of truth `shared_strategies/open/registry.py`; spot/futures shims build fresh `STRATEGY_REGISTRY` via `build_registry()`. Close evaluators live under `shared_strategies/close/` and register in `shared_strategies/close/registry.py` with `evaluate(position, market, params)`. New open strategy checklist: (1) register in `shared_strategies/open/registry.py` + `PLATFORM_ORDER`, (2) `knownShortNames` + defaults in `init.go`, (3) `DEFAULT_PARAM_RANGES` in `backtest/optimizer.py`. Use `variants={"futures":{...}}` rather than duplicating. Before refactoring registry: snapshot `--list-json` output and `diff` after — Go `discoverStrategies` depends on byte-identical output.
- `apply_strategy(name, df, params)` — config params merged UNDER runtime params (runtime priority).
- `check_strategy.py` uses manual `sys.argv` parsing — filter `--`-prefixed args before indexing. Other check scripts use argparse.
- `StrategyConfig.HTFFilter` — Go appends `--htf-filter`; not applied to options or `delta_neutral_funding`.
- `delta_neutral_funding` is perps-only — function in `spot/strategies.py` but registered only for futures.
- Perps vs futures at Position level: both `Multiplier > 0`; only perps `Leverage > 0`. Use both fields for leverage-aware metrics.
- Per-strategy DD: spot/options/futures use peak-relative; perps uses `unrealized_loss / deployed_margin` when positions open. Margin uses exchange `Leverage`, not `SizingLeverage` (#497). `RecordTradeResult` only updates `DailyPnL` and `ConsecutiveLosses` — lifetime trade counts come from SQLite, not `RiskState` (#472).
- **Test pattern:** extract pure helpers (`perpsLiveOrderSize`, `*OrderSkipReason`, `parseXxxCloseOutput`) from subprocess-spawning wrappers — Go CI lacks `.venv/bin/python3` so any test calling `RunPythonScript` / `RunHyperliquidExecute` / `RunHyperliquidClose` fails CI.
- Per-strategy circuit-breaker pending close: `RiskState.PendingCircuitCloses map[string]*PendingCircuitClose` keyed by `PlatformPendingClose{Hyperliquid,OKX,Robinhood,TopStep,OKXSpot,RobinhoodOptions}`. Use `setPendingCircuitClose`/`clearPendingCircuitClose`/`getPendingCircuitClose` accessors. `CheckRisk` takes `*PlatformRiskAssist`. RH enqueue runs only from drain's stuck-CB recovery (avoids TOTP cost). **HL perps CB drain (#515):** when `hlLiveStrategiesForCoin(sym)` reports more than one live strategy on the same coin/wallet, the Hyperliquid drain clears that strategy's pending close without issuing an on-chain close (prevents accidental full-position closes shared with peers) — `[INFO] hl-circuit-close: ... shares the wallet position with peers ...`.
- **Operator-required CBs** (`OKXSpot`, `RobinhoodOptions`): `OperatorRequired=true`; `drainOperatorRequiredPendingCloses` emits one CRITICAL log + notifier per cycle until CB resets.
- **Kill switch:** `planKillSwitchClose(KillSwitchCloseInputs)` returns pure `KillSwitchClosePlan` with `OnChainConfirmedFlat`. Adding new platform = add fields + close/fetcher pair, not signature change. OKX-spot / RH-options surface as warnings, don't block confirmed-flat. Per-platform timeouts (`HLCloseTimeout`, `RHCloseTimeout`, ...). Auto-reset on confirmed-flat: clears virtual state, resumes trading next cycle.
- **`initial_capital` immutable:** `SaveState` refuses stale in-memory overwrites. ONLY write path: `StateDB.SetInitialCapital(strategyID, value)`.
- SQLite column rename migration: use `PRAGMA table_info` to detect three states (neither, legacy-only, new-only); idempotent. `UnmarshalPendingCircuitClosesJSON` accepts both new map and legacy `{"coins":[...]}` shapes.
- Per-trade stop-loss: `EffectiveStopLossPct(sc)` (HL perps only) returns the price-% trigger. Resolution order incorporates `TrailingStopATRMult` (#507) and `StopLossATRMult` (#562): positive → returns `0` at order-placement time (`EntryATR`/`AvgCost` not yet on position); initial trigger arms on the next cycle once `stampEntryATRIfOpened` fills `Position.EntryATR` — `TrailingStopATRMult` arms via the trailing-stop loop, `StopLossATRMult` arms once via `hyperliquidArmFixedATRStopLossLive` (live) / `runHyperliquidFixedATRStopLossPaper` (paper) and is never updated thereafter. Explicit `0` falls through so e.g. `stop_loss_atr_mult: 0` plus `stop_loss_pct: 2` still arms the fixed stop. Then `TrailingStopPct` (#501), `StopLossPct`, `StopLossMarginPct/Leverage`, or `min(MaxDrawdownPct, 50)` when all preceding are inactive — sole-owner-only for the auto fallback. `LoadConfig` calls `normalizeHyperliquidPeerStopLosses` for a five-field opt-out on same-coin peers (#484/#490/#494/#501/#507/#562), then defaults sole-owner HL perps strategies with all five omitted to `StopLossATRMult=DefaultStopLossATRMult` (1.0× ATR, #562). Explicit `0` opts out where applicable; at most one positive owner among `stop_loss_pct`, `stop_loss_margin_pct`, `trailing_stop_pct`, `trailing_stop_atr_mult`, `stop_loss_atr_mult`. ATR-derived effective % ≈ `mult * EntryATR / AvgCost * 100`, capped (`MaxAutoStopLossPct`). Reduce-only trigger OID stored on `Position.StopLossOID`; `HyperliquidLiveCloser` close path takes `cancelStopLossOIDs []int64`. HL exchange caps open trigger orders at 1000 per account (not 10/day, #481).
- HL fixed ATR stop-loss (#562): `stop_loss_atr_mult` mirrors `trailing_stop_atr_mult` shape but the trigger is fixed (`AvgCost ± mult*EntryATR`), armed once on the cycle after open and never updated. Mutually exclusive with `stop_loss_pct`/`stop_loss_margin_pct`/`trailing_stop_pct`/`trailing_stop_atr_mult`. Live: `hyperliquidArmFixedATRStopLossLive` places via `RunHyperliquidUpdateStopLoss` with `currentOID=0` once `Position.StopLossOID==0` and `Position.EntryATR>0`. Paper: `runHyperliquidFixedATRStopLossPaper` sets `Position.StopLossTriggerPx` on the first cycle and books a synthetic close via `recordPerpsStopLossClose(..., "stop_loss_atr_paper", ...)` once mark crosses. Hot-reload: numeric changes take effect on next fresh open; nil↔positive toggle blocked while open. Missing-EntryATR alerts share the `atrMultMissingEntryATRWarned` throttle map with the trailing variant.
- HL trailing stop-loss live (#501/#502/#507): `%` trailing — `effectiveTrailingStopPct(...)` distance from HL `TrailingStopPct`. **ATR-mult trailing (#507)** — distance from `TrailingStopATRMult * Position.EntryATR / Position.AvgCost` (frozen at open); hot-reload can change mult when flat; nil↔positive toggle blocked while open (mirrors `TrailingStopPct`). Missing `EntryATR` emits one-shot WARN + notifier throttle per (strategy, symbol), cleared on close/reload disables. `runHyperliquidTrailingStopUpdate` walks `Position.StopLossHighWaterPx`, cancels+replaces when move exceeds `TrailingStopMinMovePct` (default 0.5%). Cancel failures NOTIFIER-alert (1000-trigger cap racing). Sizing leverage: `EffectiveSizingLeverage` vs `EffectiveExchangeLeverage` (#497); peer validation matches exchange `Leverage`, not `SizingLeverage`.
- HL trailing stop-loss paper (#532): `runHyperliquidTrailingStopPaper` reuses `computeTrailingStopUpdate` + `trailingStopBreached` to evaluate breaches in scheduler state and book a synthetic close via `recordPerpsStopLossClose(..., "trailing_stop_loss_paper", ...)`. Strategy-scoped automatically — each `StrategyState.Positions` is per-strategy, so peers on the same coin retain virtual exposure when one strategy breaches. When extending other live-only features to paper, ungate related diagnostics from `hyperliquidIsLive` (e.g. `notifyATRMultMissingEntryATROnce`) so paper and live share the same throttle map.
- HL perps margin mode: `StrategyConfig.MarginMode` (`isolated`|`cross`, default `isolated`, #486). Scheduler calls `update_leverage(leverage, coin, is_cross=...)` from flat (`posQty==0`) before opening; HL rejects mode/leverage changes on an open position. Hot-reload mirrors the `Leverage` envelope — change allowed when flat, rejected with positions open.
- Multi-strategy HL perps on same coin (#491/#494/#507): `LoadConfig` calls `normalizeHyperliquidPeerStopLosses` to coerce omitted `stop_loss_pct`/`stop_loss_margin_pct`/`trailing_stop_pct`/`trailing_stop_atr_mult` to explicit `0` for peers in a shared-coin group (so the `MaxDrawdownPct` auto-derive fallback only fires for sole-owner strategies). `hyperliquidPeerStrategyErrors` then validates that peer strategies share `margin_mode` + `leverage` and that at most one peer owns a positive stop/trailing (`EffectiveStopLossPct` / `%` trailing / ATR-mult) after normalization. `check_hyperliquid.py` calls `adapter.get_position_leverage(symbol)` and skips `update_leverage` when current state already matches; falls back to `update_leverage` on info-API errors. `validateHotReloadCompatible` mirrors the peer check. Sub-account isolation is the only correct path for full per-strategy independence.
- **SIGHUP hot reload:** `applyHotReloadConfig` (config_reload.go) re-applies a subset of config without restart. `validateHotReloadCompatible` blocks shape changes (strategy add/remove, script/args/type/platform/HTFFilter, AllowShorts mid-run, kill-switch identity, DB path); `validateHotReloadStateCompatible` blocks per-strategy `leverage` changes when positions are open. Notifier reload re-routes Discord/Telegram channels in place; guard new backends behind nil-checks.
- Drain-failure alerts: `shouldNotifyDrainFailure(key, throttleMap)` + `formatDrainFailureAlert` (failure_alerts.go); throttles per (strategy, platform, symbol, direction).
- Sharpe: per-strategy + book-level annualized; rendered in leaderboard col + `Book Sharpe` footer. `RiskFreeRate` baseline from config (explicit 0 respected). Strategy-interval speedup: when DD > `warn_threshold_pct`, returns `strategyDrawdownFastIntervalSeconds`.
- Adding a per-strategy config flag: (1) field on `StrategyConfig`, (2) main.go `run*Check` appends CLI flag, (3) parse in each Python check script, (4) `InitOptions` + wizard + `generateConfig`.
- Test helpers: name with platform/feature prefix (`stubHLLiveCloser`, not `stubCloser`) — `package main` is shared across `*_test.go`.

## Pull Requests
- Reference related issue with `Closes #<N>` in body.
- In GitHub comments avoid `#N` for list items (auto-links to issues/PRs); use `1.` instead.
- Fetch latest bot review: `gh api repos/richkuo/go-trader/issues/<N>/comments --jq '[.[] | select(.user.login=="codex[bot]" or .user.login=="claude[bot]")] | last | .body'` (top-level summary lives on issues endpoint, not pulls).
- Before merging long-running PR: `git fetch origin main && git diff origin/main..HEAD -- <paths>` to catch silent reverts; rebase if unexpected deletions.
- Replace default `🤖 Generated with [Claude Code]...` footer with metadata (model + effort + harness), e.g. `LLM: Claude Sonnet 4.6 (1M) | high | Harness: anthropics/claude-code-action@v1`. No `Co-Authored-By` trailer.
- **Claude Code** (`.github/workflows/claude.yml`): The workflow stamps `Harness: anthropics/claude-code-action@v1` (via job `CLAUDE_HARNESS`) on the patched `claude[bot]` comment footer after stripping any stale footer; optional `ccusage` token/cost metrics append last on the same line when session data exists.
- **Codex** (`.github/workflows/Codex.yml`): Job-level `CODEX_MODEL` / `CODEX_EFFORT` / `CODEX_HARNESS` (defaults e.g. `gpt-5.5`, `high`, `openai/codex-action@v1; sandbox workspace-write`). Reply comments append `---` then `LLM: <model> \| <effort> [| Harness: <harness>]`. After a successful push back to the PR branch, a `pulls.update` step rewrites **only** a tail `---`/LLM: block using an **end-anchored** regex (no `m`/multiline flag so mid-body separators stay intact); it may inject `Closes #<N>` only when Codex's final assistant message matches a line-leading `Closes #<digits>` and the PR body still lacks any closing keyword. Prompt reminds Codex to include `Closes #<issue>` when resolving issues. Tune the job env vars when changing model, effort, sandbox pin, or harness string.

### PR review format (`@claude review`)
When invoked to review a PR, the top-level review comment MUST take exactly one of two shapes — no preamble, no closing remarks:
1. **Approve:** a single line starting with `LGTM — ` followed by a one-sentence rationale (what was checked + why it's safe to merge).
2. **Changes requested:** a numbered list (`1.`, `2.`, …) where each item is one sentence naming the concrete change, with `file:line` when applicable. Order by severity (blockers first). No "nice to haves" mixed in — if it's not blocking, leave it out or put it under a final `Optional:` line.
Inline `pull_request_review_comment` threads are exempt from this format; this rule governs only the top-level review summary.

## Build & Deploy
- Build: `go build -o go-trader .` — always rebuild before smoke-testing (`cd scheduler` fails in bash tool; run commands directly from current context).
- Restart: `systemctl restart go-trader`. Service file changes: `systemctl daemon-reload && systemctl restart go-trader`.
- Config-only changes (no rebuild needed): `kill -HUP $(pgrep go-trader)` — `config_reload.go` re-reads `cfg.ConfigPath` without dropping state or sessions.
- Python script changes: take effect next scheduler cycle (no rebuild).
- **Post-update agent protocol:** after `git pull` / auto-update / when the operator says "I just updated", follow `SKILL.md` → "Post-Update Agent Protocol" — diff `<running-version>..HEAD`, classify each commit (auto-migration / runtime-default / opt-in / open-position-constrained / no-op), read `config.json` + open positions from `state.db`, and prompt per item using the template before applying. The binary's `runConfigMigrationDM` only covers `configFieldRegistry` (≤ v3); everything newer (v6/v7/v8/v10/v11 deprecations, trailing stop #502, ATR-mult field `trailing_stop_atr_mult` #507, margin_mode default #486, peer normalization #494, open/close composition #483 / registry split #511, `disable_implicit_close` removal #508, HL shared-CB drain behavior #515, Discord summary label shortening #514, regime detection `AllowedRegimes` / `cfg.Regime` #541/#546/#558) must be surfaced by the agent where it affects ops.

## Backtest
- `.venv/bin/python3 backtest/run_backtest.py --strategy <n> --symbol BTC/USDT --timeframe 1h --mode single`
- `.venv/bin/python3 backtest/backtest_options.py --underlying BTC --since YYYY-MM-DD --capital 10000`
- `.venv/bin/python3 backtest/backtest_theta.py --underlying BTC --since YYYY-MM-DD --capital 10000`
- Close-strategy registry runs in backtests (#535): `Backtester(close_strategies=[...], close_params={...})` or `--close-strategy NAME --close-params '{"name":{...}}'`. Per-bar eval against the simulated position; max `close_fraction` applied at next bar's open and combined with column-based `close_fraction` via max-wins. Entry context (`avg_cost`/`initial_quantity`/`entry_atr`) stamped at open with the same 50%-of-AvgCost ATR guard as `stampEntryATRIfOpened`. Bar-level granularity — live intra-bar trigger races (HL stop-loss OIDs) are not simulated.
- Regime detection in backtests (#549): `Backtester(regime_enabled=True, regime_period=14, regime_adx_threshold=20, allowed_regimes=[...])` or `--regime-enabled --regime-period 14 --regime-adx-threshold 20 --allowed-regimes trending_up trending_down`. Vectorized `ensure_regime_columns()` runs once before the per-bar loop; new entries blocked when `bar_regime ∉ allowed_regimes`; close paths always execute (mirrors live gate). Options backtest (`backtest_options.py`, `backtest_theta.py`) does not support regime yet.

## Testing
- **New functionality must include tests.** Go: `_test.go`. Python: `test_*.py`. Bug fixes: regression test when feasible.
- `python3 -m py_compile <file>` from repo root for syntax check.
- `go build .` (compile) / `go test ./...` (unit tests — `cd scheduler` fails in bash tool; run commands directly).
- `gofmt -w <file>.go` after editing (no `cd` needed).
- Multi-line Go edits with tabs: Edit tool may fail; use heredoc form: `python3 << 'PYEOF'` / `content=open(f).read()` / `open(f,'w').write(content.replace(old,new,1))` / `PYEOF`.
- Strategy listing: `cd shared_strategies/open/spot && ../../../.venv/bin/python3 strategies.py --list-json` (worktrees: absolute path to main repo's venv). Futures listing lives at `shared_strategies/open/futures/strategies.py --list-json`.
- Smoke tests:
  - `./go-trader --once` / `./go-trader --config scheduler/config.json`
  - Interactive init: `printf "answer1\nanswer2\n" | ./go-trader init`
  - JSON init: `./go-trader init --json '{"assets":["BTC"],"enableSpot":true,"spotStrategies":["sma_crossover"],"spotCapital":1000,"spotDrawdown":10}' --output /tmp/test.json`
  - Status port override: `./go-trader --once --status-port 9100` — verify `[server] Status endpoint at http://localhost:<port>/status`.
- Pytest: `.venv/bin/python3 -m pytest shared_strategies/ -v`; also `shared_tools/`, `platforms/`, `backtest/` (run when modifying strategies). `uv run pytest <relative-path>` may fail if bash CWD differs from repo root — use `.venv/bin/python3 -m pytest` instead. `shared_scripts/test_*.py` is NOT in default `testpaths` — invoke explicitly.
- When adding tests that touch sys.path or registries, run the FULL suite (`.venv/bin/python3 -m pytest backtest/tests/ shared_tools/ shared_strategies/`) — isolated single-file runs can mask import-order conflicts (e.g. open vs. close `registry.py` collision).
- `stampEntryATRIfOpened` rejects ATR > 50% of AvgCost (plausibility guard); Go tests using `atr` indicators must use values < 50% of the entry price in the test.
- Strategy tests must assert actual signal values (`assert (result["signal"] == 1).any()`), not just column existence. Smoke tests iterating registered strategies need a `DatetimeIndex` (`amd_ifvg` reads `index.hour`, `vwap_reversion` buckets by `index.date`).
- Python test imports: use `importlib.util.spec_from_file_location` (avoids two `strategies.py` collisions).
- Go tests: always check `json.Unmarshal` errors — silent discard masks struct tag/type regressions.
- Pure helpers (`perpsLiveOrderSize`, `*OrderSkipReason`, `effectiveStrategyIntervalSeconds`, `parseXxxCloseOutput`, Sharpe math in `sharpe_test.go`) are testable without subprocesses — use this pattern for any subprocess-spawning live helper.
- **ATR for close evaluators:** `tiered_tp_atr` and `trailing_stop_atr_mult` depend on `Position.EntryATR` stamped from `indicators["atr"]`. All check scripts inject a default ATR(14) via `ensure_atr_indicator(result_df)` from `shared_tools/atr.py`; call it before the indicator loop AND re-capture `last = result_df.iloc[-1]` afterward — `last` is a frozen snapshot taken before the in-place mutation and won't include the injected `atr` column otherwise (#536/#537). New check scripts must follow this ordering. `tiered_tp_atr_live` (#527) recomputes the tier ruler each tick from `market_ctx["atr"]` (populated by `latest_atr(df)` in every check script: `check_strategy.py`, `check_hyperliquid.py`, `check_okx.py`, `check_robinhood.py`, `check_topstep.py`); param `atr_source` (`"live"` default | `"entry"`) toggles live vs. snapshot, and warm-up gaps fall back to `position.entry_atr` so a missing live ATR doesn't deadlock the close. Discord summary TP1/TP2 hints always price from `EntryATR × {1,2}` regardless of `atr_source` (#529). When `tiered_tp_atr` is configured but the open candle doesn't produce ATR, `tieredTPATRMissingEntryATR` + `notifyTieredTPATRMissingEntryATROnce` (`hyperliquid_trailing_stop.go`, #525) emit a one-shot WARN + notifier alert per (strategy, symbol) — same throttle map as the ATR-mult trailing alert. `stampEntryATRIfOpened` rejects ATR > 50% of `AvgCost` (and NaN) as a plausibility guard.
