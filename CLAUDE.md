# go-trader Project Context

## Environment
- Go 1.26.2; full binary path if not on PATH (`/opt/homebrew/bin/go`).
- Python via `uv run --no-sync python` for dev/backtest/manual CLI; Go subprocess (scheduler) calls `.venv/bin/python3` directly — deterministic after `uv sync`, no systemd PATH config. Worktrees: `uv sync` once per checkout.
- **systemd:** `go-trader.service`, `systemd/go-trader@.service` use `ProtectSystem=strict`; no `PATH`/`UV_CACHE_DIR` injection. `/opt/go-trader/.env` (or per-instance) sets `DISCORD_BOT_TOKEN` + secrets. **#1056** config lives OUT of the deploy tree at `/var/lib/go-trader[/<instance>]/config.json` (`StateDirectory=go-trader[/%i]`, writable under `ProtectSystem=strict`); `ExecStart … --config <that path>`. `WorkingDirectory` stays the code checkout. `scheduler/config.json` is a transition **symlink**; migrate with `scripts/migrate-config-out-of-tree.sh`. **#1431** the template unit also grants `StateDirectory=go-trader/shared` — the cross-instance writable dir for the live→paper `replay_log_path`.

## Setup
- `uv sync`; copy `scheduler/config.example.json` → `scheduler/config.json`, fill API keys. **#1056 (deployments):** see Environment above — stop the service, run `scripts/migrate-config-out-of-tree.sh` once (refuses while the daemon is live, else a config write in the migrate→restart window clobbers the symlink into an in-tree file), then point `ExecStart --config` + `StateDirectory` at it.

## Decision Priorities
- **Always pursue the absolute best technical solution — full stop.** Cost, compute, resources, time-to-implement, effort, manpower, tests, and code volume are NOT design constraints and must never narrow the option space: choose the best solution as if these were unlimited, then update/rewrite tests after the feature is done. These govern *quality*, not *scope*; don't override the branch+PR workflow, "verify issue claims against code", or destructive-action safety rules.
- **Never give time, duration, or effort estimates** ("2–4 days", "low effort", headcount/manpower) in responses, PRs, issues, or commits. Describe complexity only in terms of scope and risk.

## Repo Structure
`scheduler/` is one Go `package main`. **Guardrails only below — full file-ownership map, symbol/config inventories, and per-feature mechanics live in `docs/ARCHITECTURE.md` (not auto-loaded; read the relevant section there before editing a subsystem).**
- `executor.go`/`shutdown.go` — **New side-effecting wrapper → `runPythonSideEffect`, NEVER `runPython`.** **Detail → ARCHITECTURE.md.**
- `server.go`/`ui_*.go`/`static/ui/*` — **Lock order `mu → strategiesMu`** (reusing `mu` deadlocks SIGHUP). **Loopback only**; `applyStrategyConfigPatch` needs `config_version>=13`. **#1340** `/tuning` (`ui_server.go`, `static/ui/tuning.html`) is a read-and-launch page for #1339 runs — re-reads `/api/strategies/<id>/config` on every poll so diffs/baseline banners always reflect live config; never writes config. **Detail → ARCHITECTURE.md.**
- `ui_tuning.go` (#1339/#1382/#1341) — `/api/tuning/runs` suggest-only lane via `spawnPythonProcessWithEnv` (NEVER `runPython*`); `tuning.max_retained_runs` prunes terminal dirs; `POST /api/tuning/apply` sole promotion (drift-refused vs schema-v2 `promotion_baseline`, `mutateConfigRoot` exact replace). **Mechanics → ARCHITECTURE.md.**
- `config.go`/`config_migration.go` — `CurrentConfigVersion=17`. `MinSupportedConfigVersion=13` (fail-loud at load; `scripts/check-config-versions.sh` must show fleet ≥ floor first). Seven mutually-exclusive HL stop fields (all-omitted→`DefaultStopLossATRMult=1.0`); single `*StrategyRef` (`close_strategy` canonical); unknown-key guard; `strategyUsesTieredTPATRClose(sc)` gates on-chain TPs, NOT `len(tiers)>0`; `CircuitBreaker *bool` read ONLY via `CircuitBreaker*` accessors. **→ ARCHITECTURE.md.**
- `close_defaults.go` — **#866/#1135** three-layer resolution (system→user→strategy); explicit `tp_tiers` wins; reserved `regime_atr` section for standalone `*_atr_regime` `use_defaults` owners (#1134). **#1133** `user_defaults.close["trailing_tp_ratchet_regime"]` may carry `trailing_stop_atr_regime` — `applyUserCloseDefaultRatchetRegimeTrails` runs in `loadConfig` **before** the scalar ATR-stop default. **Mechanics → ARCHITECTURE.md.**
- `state.go`/`db.go` — SQLite-only (`modernc.org/sqlite`); idempotent migrations. **#1395** `LoadState` bounds per-strategy trades in SQL (`ORDER BY timestamp DESC, rowid DESC LIMIT maxTradeHistory` + `idx_trades_strategy_timestamp`). `ValidatePerpsDirectionConfig` startup check; `CheckStatePresence` (`GO_TRADER_ALLOW_MISSING_STATE=1`).
- `risk.go`/`strategy_interval.go` — `CheckRisk` skips `manual`. **#1009** corrupt position (qty≤0 OR avgCost≤0) → **zero-PnL** `*_corrupt` close leg, cash untouched (`closePositionIsCorrupt`/`bookPerpsCloseWithFillFee`). **#1008** `classifyPositionTradeType` labels force-close legs (operator-display only). **#1444** `collectPerpsMarkSymbols` also feeds `type=manual` (keyed on `sc.Symbol`, not `Args[1]`) — before this fix a manual coin with no perps/hedge donor never got a mark, so the HL trailing-SL walker and TP ratchet died silently on `mark>0` gates; manual now values at live mids in `PortfolioValue`/exposure/drawdown. One-shot `PortfolioRisk.PeakValue` basis migration (`portfolio_risk.manual_mark_basis_rebaselined` latch) reconciles the pre-fix `AvgCost`-based peak so the kill switch doesn't false-fire on upgrade. **#1448** the portfolio latch is owned by ONE signal per cycle: `equityGuardArmed = equityAvailable && PeakValue>0` (read AFTER the peak ratchet) ⇒ trips on equity DD only (`Source="equity"`); not armed (pooled wallet with no trustworthy balance, or a cold start that never recorded a valuation) ⇒ trips on margin DD (`Source="margin"`). Margin-wins tie-break deleted. Margin DD above `max_drawdown_pct` is now a reachable WARN state (its own wording; never "approaching") — **#1449 review** throttles that warn DM (`portfolio_warning_throttle.go`; first-cycle / newly-in-band signal / ≥1.0pp rise / `alert_throttle_interval` floor, reset when the band clears) because it can now persist forever. **#1449 review** `equityTrusted` (`pooledEquityComplete && !usedPVFallback && !usedStaleRiskBalance`) is a THIRD state: the latch STAYS with equity on a substituted/stale total (handing the book to margin over a fetch blip is the #1448 failure), but the peak ratchet is skipped and equity DD is FLOORED at the last reading so an overstated substitute cannot mask a loss. **The floor alone can never latch because it is CLAMPED to `max_drawdown_pct` at read time** — the latching cycle persists its OVER-LIMIT reading (write precedes the latch check) and the #1368 owner-DM reset used to leave it, so an unclamped floor re-latched the book after a reset or a restart; a real this-cycle measurement above the limit is unclamped and still latches. `ResetPortfolioKillSwitchManual` is now the sole DM-reset body (clears both readings + marker, returns the pre-reset value for the audit event, never re-baselines the peak) — matching both auto-reset paths. The floored value stays persisted in `CurrentDrawdownPct` (so the stored number equals the one that cycle decided on) and is therefore MARKED by `DrawdownReadingSubstituted` (new additive column `portfolio_risk.drawdown_reading_substituted`); every operator surface printing it beside peak/total must label it. `PortfolioWarningMessageInputs.EquityGuardArmed` makes the warn message name the arm that can actually latch — labels SWAP when the guard is unarmed (margin owns the latch there) and the equity figure renders `n/a` instead of a stale reading. Per-position margin protection stays with the #292 per-strategy circuit breaker — **which is opt-outable** (`circuit_breaker:false`, #1048) at a LOOSER perps default (50 vs portfolio 25), so `recordCircuitBreakerSuppression` now raises a throttled owner DM (`circuit_breaker_suppression_alert.go`, queued under `mu`, drained by the main loop) instead of a log line only. `portfolioWarnBandSignals` is the SINGLE warn-band definition (risk check + throttle). **#1449 review round 3** the floor closes only the fail-open direction; an untrusted total that UNDERSTATES equity inflated `equityDD` and could flatten the whole book (manual+spot) off a distrusted number. The equity arm now gets the floor's own guarantee — an untrusted over-limit reading does not latch on the cycle it appears — but as a **DEFERRAL with a deadline, never a veto**: `usedStaleRiskBalance` is structurally ONE cycle (cache not refreshed ⇒ next miss drops to `equityAvailable=false` where margin latches), while `usedPVFallback` can persist for a whole balance-endpoint outage, so a pure veto would disarm the only full-book protection exactly when a crash is least likely to be noticed. `UntrustedOverLimitSince` (persisted, additive column `portfolio_risk.untrusted_over_limit_since`) times an **UNBROKEN** run of untrusted over-limit cycles; held while `< untrustedEquityLatchDeferral` (15m, NOT configurable), then latches naming the untrusted basis. **Every non-qualifying cycle clears it** (trusted / at-or-below limit / guard unarmed), as do the firing latch and all three resets — persistence is what stops a crash-restart loop reopening the window forever. `MaxDrawdownPct<=0` excluded (keeps its "latch on any drawdown" meaning). Margin arm and #292 untouched — they are the standing backstop. **Loud, not silent:** `latch_deferred` audit row (once per window), `[CRITICAL]` stdout, warn DM **bypasses** the throttle, warning message replaces the misleading `Distance to kill switch: 0.0% equity` line, `/circuit-breakers` shows `Portfolio latch DEFERRED`. Deferred cycles persist their OWN unclamped measurement. **Round-2 test assertions moved:** "genuine over-limit latches immediately" now pinned on a TRUSTED cycle (that was the clamp's non-fail-open property); untrusted is defer-then-escalate. No config change (`CurrentConfigVersion` stays 17); two additive SQLite columns. **Mechanics → ARCHITECTURE.md.**
- `daily_loss.go` — portfolio-wide daily loss limit (`portfolio_risk.daily_max_loss_usd`/`daily_max_loss_pct`, 0=off; both set → lower resolved USD wins; pct basis = Σ strategy `initial_capital`). **Hold-only, UNLATCHED pure read**; measures PRE-FEE realized PnL; never force-closes. **New `portfolio_risk` gate → copy this shape** (RLock eval, `pausedBlocksSignal` holds, `manualStateView` refusals, `clonePortfolioRiskConfig` hot-reload). **Mechanics → ARCHITECTURE.md.**
- `exposure_cap.go` — same-direction exposure cap (`portfolio_risk.max_same_direction_notional_usd`/`max_asset_concentration_pct`, 0=off). **Blocking-only + direction-aware** (`exposureCapBlocksSignal`); TS futures site ungated. **Single exposure model** — `computeAssetDeltas` (correlation.go) shared with `ComputeCorrelation`. Hot-reloadable via SIGHUP (unlike `max_notional_usd`). **Mechanics → ARCHITECTURE.md.**
- `notional_cap.go` (#1344) — portfolio gross notional cap (`portfolio_risk.max_notional_usd`, 0=off). **Hold-only via `pausedBlocksSignal`** — never skip the strategy cycle (`notionalCapSkipsStrategyCycle` always false). Closes/SL/TP maintenance keep running; manual open/add/limit-open refuse. Restart-required. **Mechanics → ARCHITECTURE.md.**
- `replay_log.go`/`replay_mirror.go` (#1431) — **DEFAULT-OFF** live→paper decision replay. Root `replay_log_path` (restart-required; shared SQLite OUTSIDE every deploy tree) + per-strategy `replay_sharing="live_mirror"` (HL perps only, Go-only flag like `paused`, hot-reloadable flat-only). Live writes open/scale_in/partial_close/full_close rows (with live's EntryATR/regime stamps) at the recording choke points; a paper strategy with the same `id` suppresses its own position-increasing signals (`pausedBlocksSignal` arm in the HL perps dispatch) and replays rows (opens book exact live qty+VWAP; full closes book at paper's mark with reason `replay_live_mirror`). **Apply (eager InsertTrade + diagnostics suspended so those rows join the tx) → `SaveStrategyBookWithDB` (THIS strategy's book + trades + deferred diagnostics + `strategies.replay_mirror_watermark` in one tx; does not rewrite the rest of the fleet) → `MarkDecisionsApplied`, in that order** — the persisted watermark makes post-crash re-marks idempotent. **Book-drift rows (open-while-holding / scale-in-while-mismatched / partial-close-while-flat) consumed with WARN + throttled owner DM, never retried; close-while-flat is INFO.** **Mechanics → ARCHITECTURE.md.**
- `hl_batch.go` (#1442) — batched HL signal checks: due strategies sharing `hlBatchKey{DataPlatform,Symbol,Timeframe,OhlcvLimit,ATRMethod}` run in ONE `check_hyperliquid.py --batch-check` call (slots on stdin). Everything off-key travels per slot. **`partitionHyperliquidBatchGroups` is PURE** (no locks/globals/IO) over `dueStrategies` only; **the slot snapshot is ONE `mu.RLock` read AFTER `reconcileHyperliquidAccountPositions`, never before** (pre-reconcile position ctx into close evaluators). Group of one → no batch. `hyperliquidBatchSlotFingerprint` is re-checked at dispatch — mismatch ⇒ that strategy spawns its own check. **The cycle mark price is NOT in the fingerprint** (`--mark-price` sets only the REPORTED price; the decision reads the last closed bar) — folding it in let the first member's `round(mid,2)` write-back invalidate every peer, so the cycle batched AND re-spawned N-1; the dispatch-time mark is re-stamped on a COPY of the cached result via `hyperliquidBatchDisplayPrice` instead. Both paths finish via `finishHyperliquidCheck`, so downstream (risk/execution/#1431 replay/hedge) is unchanged. **A shared-state failure leaves every member a map MISS** — each takes its own spawn in the SAME cycle, so a batch-path fault can never blank close eval / trailing SL / ratchet / protection / hedge sync for the whole coin; member trackers stay frozen and ONE alert fires on `hlBatchAlertConfig` as `**HL BATCH SHARED STATE FAILED**`. Per-slot errors stay per-member. 3 consecutive shared failures ⇒ group reverts to per-strategy (retry every 10 cycles). `GO_TRADER_HL_BATCH=0` disables; `CurrentConfigVersion` stays 17. Go tests stub `runHyperliquidBatchCheckFn`. **Mechanics → ARCHITECTURE.md.**
- `hyperliquid_fills.go` — fill resolver built **outside `mu.Lock`** (failure→modeled fee); `HLFillLookup.Px`=VWAP.
- `pause.go` (#1150) — `StrategyConfig.Paused` (`"paused"`); NOT a `dueStrategies` skip, full manage-only cycle. `pausedBlocksSignal(...)` holds position-increasing signals at all 6 regime-gated dispatch sites; closes/trailing SL/ratchet/protection sync pass through. Hot-reloadable always incl. while open. **Mechanics → ARCHITECTURE.md.**
- Regime cluster (`regime*.go`, `post_tp_sl.go`) — **detail → ARCHITECTURE.md:**
  - Stamp on all 5 execute dispatches; store failure → display `regime=-` (#879); ENTRY empty-label policy via `resolveRegimeGateOnFailure` (#1278 `"open"`|`"closed"`).
  - `regime_atr.go`: strip `sl_after` before ATR parse; v15 `atr_multiple` canonical. `regime_unified.go` (#841) owns SL. Directional policy DEFAULT-OFF + exact-match certs (no family fallback). Profile allocation flat-only (#998). `#1189` display-only label. Dynamic/divergence HL-live-only. Transitions (#1224) alerting-only.
- `trailing_tp_ratchet.go` — **NO on-chain TP.** SL via `trailing_stop_atr_mult`/`trailing_stop_atr_regime`; HL perps+`manual`; hot-reload blocked while open; DM gated by `notify_ratchet_triggers` (#1118). **#1416** same-cycle tier tighten (scale-out ratchet or scale-in add) cancel+replaces resting SL immediately — one-shot bypass of `TrailingStopMinMovePct` debounce.
- `hedge.go` (#1159) — per-strategy correlated hedge legs, **HL perps only (live+paper), phase 1**. **ONE state-derived reconciler is the only mirror point** — `hedgeTargetDecision` (pure) + `runHedgeSync` converge the hedge to a target derived from the primary's qty vs `Position.HedgePrimaryQtyBasis`; never add per-event hedge hooks. Qty-event sizing (mark drift never re-trades). **Ownership is `Position.HedgeFor` ONLY** — never coin→config inference; an unstamped position on the hedge coin is never adopted. **`validateHedgeConfigs` collision matrix is load-bearing**: a hedge coin must be nobody's primary and no other hedger's — that is what lets every `hyperliquidConfiguredCoin`-keyed shared-coin mechanism stay blind to hedge coins. **Hedge PnL → `RecordHedgeTradeResult`, NEVER `RecordTradeResult`** (a hedge loses when the primary wins; the loss streak must not see it) — route via `recordPositionTradeResult`. `trade_type="hedge"` excluded from lifetime #T/W-L both sides. Fail-closed: hedge failure on a cycle that added primary exposure unwinds exactly that increment + CRITICAL DM. Hot-reload blocked while EITHER leg is open; backtester rejects an enabled block. **Mechanics → ARCHITECTURE.md.**
- `hurst_gate.go` (#1411) — per-strategy Hurst entry gate + persistence-scaled sizing, **DEFAULT-OFF, opt-in only** (live evidence is the **#1424 resolution study** at the contract path `backtest/research/hurst_gate_calibration.md`, owned by `research/hurst_1424_gate_resolution.py`; the superseded #1422 and #1410 renders moved to `hurst_1422_gate_power.md` / `hurst_1410_gate_calibration.md` and NEITHER script may write the contract path. Verdict **INCONCLUSIVE** and the study's own **VALIDITY GATE FAILED**. #1424 combined all three power routes — 1 pre-registered hypothesis, 8 added pre-2020 calendar clusters from Bitstamp + Coinbase, and a bounded-variance target (signed 96h efficiency) — for ~29k primary rows. The gate is read ROW-MATCHED and DIRECTIONALLY on the CONFIRMATORY family (`momentum`, 7,992 rows): limit 0.013 eff / 0.94 pp, separation **-0.005 eff / -0.12 pp**. **The SIGN is the finding** — every null and injection is one-sided on `mean(kept) - mean(suppressed)`, so a negative separation means the SUPPRESSED trades did BETTER, a direction the design cannot detect at any size; the run carries NO bound either way and is NOT proof of absence. Pre-registered key risk UNRESOLVED. Pinned hypothesis cluster p=0.3557 primary-target / 0.3387 net-return — #1422's interim look of 0.0485 on a subset did NOT reproduce. **The gate must never read the POOLED primary limit** (0.008 over both families' 28,998 rows) — a bigger pool resolves a smaller effect, so pairing it with one family's separation biases the gate toward passing. Separations are read POOL-MATCHED and SIGNED, never whole-study vs sub-cohort and never `abs()`. Ship NO recommended thresholds; `config.example.json` stays clean. #1424 re-discharges #1412 Stage 0 on the larger pool: `NO JOINT SEPARATION`. **#1426** re-tested the same confirmatory hypothesis TWO-SIDED (the negative separation is a direction a one-sided design can't reject) — still **INCONCLUSIVE** (two-sided p=0.4878, gate failed 0.013 vs -0.0046 on 8,009 rows), exploratory-only, defers the contract path back to #1424, ships no threshold). **Standalone gate ON TOP of the label gate** — `applyRegimeGate`/`resolveRegimeGateOnFailure`/`map_composite_label` byte-identical. Holds position-increasing signals only, via `pausedBlocksSignal` at all 6 `applyRegimeGate` sites; closes/SL/ratchet/protection/hedge/kill-switch always pass. `resolveHurstGateOnFailure` is the SINGLE accessor (#1278 mirror); fail-closed is FLAT-ONLY. **`metrics["hurst"]` comes ONLY from the composite classifier** — load-time REJECT for adx/missing/single-window (else `on_failure` silently governs forever). H is NOT bounded to (0,1) at runtime (DFA reads ~2.0 on smooth series). mode=size multiplier `clamp(|H-0.5|/0.15, floor, 1.0)` is **≤ 1.0 always**, applied where size is COMPUTED (`PerpsSizing.EntrySizeMult` in `PerpsOpenNotionalSized`; `*SignalWithFillFeeSized*` variants) — never after exposure-cap or execution. Hysteresis persists in `strategies.hurst_gate_state`, keyed by a threshold hash (edit ⇒ discard, never reuse). Hot-reloadable always incl. while open. `CurrentConfigVersion` stays 17. Backtest parity in `backtest/hurst_gate.py` — **keep `hurst_gate` in `run_backtest.py`'s `stop_keys` allowlist** or the `--config` CLI silently drops the gate. **Mechanics → ARCHITECTURE.md.**
- `llm_entry_analysis.go`/`llm_review.py` (#1137) — advisory-only; `spawnPythonProcess` NEVER `runPython*`; verdict → `trade_diagnostics.llm_verdict` (#1147 sole writer).
- `scale_in.go` (#873) — freeze geometry via `RiskAnchorPrice` (not blended AvgCost); HL perps+manual; **#1276** backtested.
- `manual.go`/`manual_limit.go`/`manual_sl.go` — kill-switch+CB; SL edits queue `PendingManualAction` (NEVER direct UPDATE); `force-close` live HL perps only. **→ ARCHITECTURE.md.**
- `hyperliquid_liquidation_guard.go` (#1450) — SL vs HL liquidation price. `HLPosition.LiquidationPx` decoded from the Phase-1 `clearinghouseState` fetch; `main.go` builds the coin-keyed `hlLiquidationPx` cycle map beside `hlOnChainAbsQty`. **CLAMP, never refuse to arm** (refusing leaves a live position unprotected) and never alert-only. **ONE-WAY TIGHTEN** at a 0.5% buffer inside `liquidationPx`; `clampStopInsideLiquidation` NEVER returns 0 for a positive trigger. **Compare ONLY against the current-cycle exchange-reported value — never persist it (no `liquidation_px` column), never derive a band from `1/lev` or `1/(2·maxLev)`; 0 means unknown and every check skips.** Healing: trailing owners via `trailingReplacePolicy.liquidationPx` (bypasses the min-move debounce); fixed/regime/unified via `buildHyperliquidProtectionPlan(sc, pos, liquidationPx)` rewriting `slMult` + `ForceSLReplace` (**OR-ed, never assigned**, at both sync branches). **#1456 review** the multiple rewrite is **DIRECTIONAL** (`anchor-clamped` long / `clamped-anchor` short via the single `hlProtectionSLTriggerPx` mirror of the Python formula) — `math.Abs` mirrored a far-side clamp (reachable in CROSS margin, where liquidation can sit past the frozen anchor) back across the anchor and re-forced an unfillable order every cycle; an unclampable geometry now REFUSES the rewrite and sets no force. `ForceSLReplace` fires only when the trigger the plan would rest is **strictly tighter** than what rests (`hlTriggerStrictlyTighter`, 0.05% tick-rounding slack), so a landed clamp converges in one replace. **The pre-dispatch `runHyperliquidLiquidationAudit` tightens EVERY owner, not only static scalar ones** — the walker and `runHyperliquidProtectionSync` both sit inside the `dueStrategies` dispatch gated on `result.Signal == 0`, so deferring to them left a stop unreachable for up to a whole strategy interval; the audit is one-way tightening and pre-dispatch, so the owner's own path finds it reachable and leaves it alone. `hlLiquidationClampReplace` returns a **tri-state outcome**: cancel-landed-placement-rejected is `protection lost` (state zeroed, CRITICAL DM), **never** a clamp. A **static scalar owner with NO resting stop is RE-ARMED** by the audit at its configured distance (nothing else re-arms it). The audit REFUSES to touch a coin whose recorded size across live strategies exceeds the on-chain snapshot (`hlLiquidationCoinBookConsistent`) — a phantom position on a shared coin would move a reduce-only trigger against a PEER's real size. Audit-booked closes return `CloseDetails` so they raise the SAME trade notification every other close path does. Open-cycle arms pass 0 and heal next cycle. Throttle keys on the **action** (any change re-alerts, incl. deferred→protection-lost) and is cleared on close via `clearHLPerpsPositionAlertThrottles` — **put new per-(strategy, symbol) throttles there.** **#1456 review round 2** the no-false-success rule now covers ALL THREE clamp sites, not just the audit: the trailing walker separates `restingConfirmed` (OID > 0, or filled-at-submit) from `updateConfirmed` (which still includes cancel-without-rest, because state must stop pointing at a dead OID) and reports `protection lost`, and the one-shot fixed-ATR arm alerts AFTER the placement via `hlLiquidationArmClampAction` (**no `defer` at that site — the dispatch body runs directly inside `main()`**). `notifyHLStopPastLiquidation` splits into a pure `hlLiquidationAlertMessage`; the CRITICAL log for an unprotected position is NOT throttled (only the DM is), and the past-liquidation preamble is emitted only when BOTH prices are known, so a stopless candidate never reads `$0.0000 is past ... $0.0000`. `stop_loss_trigger_px` (`run_sync_protection`) is now emitted ONLY when that sync PLACED an order — an echoed OID reports nothing and `applyHyperliquidProtectionSync` keeps the recorded trigger, which is what stops the audit's healed far-side trigger being overwritten by the plan's unreachable derived price and re-fought every due cycle. `hlLiquidationCoinBookConsistent` also passes a coin with exactly ONE live recorded owner (no peer to harm; `hlSLEffectiveQty` caps to on-chain), but never one absent from the snapshot. `runHyperliquidLiquidationAudit` takes `snapshotFetched` and returns immediately when the Phase-1 `clearinghouseState` fetch failed — empty maps are "no snapshot", never "every position is a phantom". Boot check `validateHLStopWithinBankruptcyBound`: pct owners only, live+isolated, `>= 100/leverage` rejected; fleet preflight `scripts/check-hl-stop-bankruptcy-bound.sh` (pinned to the Go check by a parity test). **#1456 review round 3** `LiquidationPx` decodes as `json.RawMessage` + lenient parse (`parseHLLiquidationPx`) so ANY shape lands 0 instead of failing the whole snapshot; two new alert actions — `exited` (fill-at-submit = FLAT, incl. from `hlLiquidationArmClampAction`) and `SL filled` (new `hlReplaceFilledExternally` outcome for `StopLossFilledExternally`, audit + walker); the past-liquidation lecture rides only measured+open geometry via pure `hlLiquidationAlertFullMessage`, and re-arm detail omits an unknown liquidation price; boot bound DROPS `trailing_stop_pct` and COVERS the `max_drawdown_pct` fallback (`hlStopLossResolvesFromMaxDrawdownFallback`, MaxAutoStopLossPct-capped) with the preflight mirrored in the same PR; audit-booked immediate closes persist `liquidation_clamp_sl_immediate` ("Liquidation-clamp SL close") via the new `closeReason` param on `applyTrailingStopUpdateResult`. **#1456 review round 4** the boot bound reads the regime blocks with the raw-aware `IsConfigured()`, NEVER `!IsZero()` — `validateHLStopWithinBankruptcyBound` runs in `validateConfig`'s per-strategy loop, BEFORE `validateRegimeATRConfig` calls `ResolveSurface`, and `IsZero()` reports true on an unresolved-but-configured block (#735.1/#1111 hit the same phase ordering), so a strategy whose stop is owned by `stop_loss_atr_regime`/`trailing_stop_atr_regime` looked ownerless, the `max_drawdown_pct` fallback was scored as the owner, and this FATAL bound refused to boot a legal config. `scripts/check-hl-stop-bankruptcy-bound.sh` now mirrors **LoadConfig's stop-owner resolution**, not the raw per-strategy keys — an explicit-zero `stop_loss_atr_mult`/`trailing_stop_atr_mult` falls THROUGH to the fallback (the preflight used to clear a config the restart then rejects), the #562/#601/#605 `default_stop_loss_atr_mult` auto-default attaches an ATR owner to any HL perps strategy with no stop field set, and the #1133 `user_defaults.close` ratchet-regime trail attaches a `trailing_stop_atr_regime` absent from the strategy's own JSON (the last two were false alarms that would block a healthy update). **The parity tests drive the Go side through `LoadConfigForProbe`, never a bare `validateConfig`** — validateConfig runs last in `loadConfig`, so comparing against it pins the script to a state the daemon never boots from. **#1456 review round 5** `hlLiquidationPx` is a NET per-coin price (HL nets one position per coin), so every consumer reads it through `hlLiquidationPxForSide(hlLiquidationPx, hlNetSideByCoin, coin, pos.Side)` — a strategy whose own recorded side disagrees with the on-chain net side (shared long+short coin via bidirectional perps, or a stale sole-owner side) gets 0/unknown and is never clamped, re-armed, or alerted against a price that describes someone else's leg; the net-side map is built beside `hlLiquidationPx` from the sign of `HLPosition.Size` and threaded into the audit, `runHyperliquidProtectionSync`, `scaleInResizeTrailingSLNow`, `runTrailingStopUpdateAfterRatchetTighten`, and both walker policy sites. Unprotected-position alerts state the ACTUAL recovery path via `hlLiquidationUnprotectedRecovery(sc)`: static scalar owners get "the scheduler re-arms it on the next cycle", everything else names its next due manage-only cycle (interval when configured) — the audit deliberately skips `unprotected && !staticScalar`. **#1456 review round 6** the net-side map is recorded for ANY on-chain size (independent of a reported liquidation price) and the audit's re-arm requires the confirmed side — a stale recorded side gets NO placement and no unthrottled CRITICAL, while a matching side with unknown geometry still re-arms; when an audit tighten CANCELS a stop and the replacement does not rest, `hlLiquidationRetryPlace` retries the placement ONCE in the same cycle (`cancelOID=0`, same clamped trigger/capped qty — the audit created the window, so it may not hand it to the owner's slower cadence); the boot bound does NOT score `stop_loss_pct`/`stop_loss_margin_pct` under a unified regime close (`EffectiveStopLossPct` returns 0 there — the preflight mirrors), which is error-surface hygiene only since `validateUnifiedCloseSoleOwner` already rejects that combination at load. **#1456 review round 7** the trailing walker's clamp branch gets the SAME in-cycle retry guarantee as the audit: a liquidation-clamp cancel that lands with nothing resting triggers one fresh placement via `hlLiquidationPlaceFresh` (the lock-free core `hlLiquidationRetryPlace` now delegates to — the walker already holds the coin lock), adopting a rested/filled retry result and reporting clamped/exited; an ordinary (non-liquidationPx) walker replace keeps today's no-retry behavior (`clampTriggered`). **#1456 review round 8** `hlLiquidationCoinBookConsistent` compares the SIGNED virtual net per coin against the on-chain |net| — HL nets one position per coin and long+short peers on one coin are documented-legal, so magnitude-summing made that book permanently "phantom" and refused the audit's heal forever; only a signed-net excess (real same-side drift) refuses, sole-owner route unchanged. `applyManualAction`'s full-close branch now calls `clearHLPerpsPositionAlertThrottles` like every other close site — a reopen no longer inherits a stale liquidation-alert throttle. **#1456 review round 9** the protection-sync force-replace (`check_hyperliquid.py` `run_sync_protection`) emits `cancel_stop_loss_succeeded` (false when the CANCEL failed — old order may still rest); `applyHyperliquidProtectionSync` clears dead `StopLossOID`/`StopLossTriggerPx` when the flag is set and no OID replaced it, and `hlProtectionLostExchangeStop` raises a distinct CRITICAL DM (cancel-landed + place-failed is not a TP-only hiccup) — fixed/regime/unified-ATR owners self-heal via the next sync's empty-OID path. Opposite-side phantom cancellation in the signed book check is documented as undetectable (single netted figure, #258 precedent), bounded by reduce-only clipping + `hlSLEffectiveQty` + net-side-only placement. **#1456 review round 10** `_classify_cancel_response` makes ALL cancel paths response-confirmed (HL reports rejected cancels in a normal body, not an exception); an unconfirmed cancel defers the replacement (`run_sync_protection` force-replace, `run_update_stop_loss`, execute-path stale-SL) instead of stacking a second stop, and an error payload carrying a LANDED cancel is classified protection-lost at BOTH the walker and `hlLiquidationClampReplace` (in-cycle retry runs; never "replace deferred" while nothing rests). Audit-booked closes now converge hedges same-cycle via `convergeHedgesAfterAuditClose` — the ONE reconciler invoked from a second call site for non-due owners. **#1456 review round 11** `stop_loss_outcome_unknown` (unreadable placement response / post-submit exception) gates BOTH in-cycle retries (`hlLiquidationMayRetryReplace` for the audit; the walker's clamp-retry condition) — only a positively rejected placement may retry; and `runHyperliquidProtectionSync` now returns `(synced, immediateFillPx)`, booking a fill-at-submit SL close at the trigger price on the same cycle (`protection_sync_sl_immediate`) with the standard trade line. **Round 12** `recordPositionOpen` moved to immediately after the deferred-open execute leg (before the post-trade sync / ratchet-tighten walker / #885 inline arm can submit-fill-close and delete the position), so an open-then-instant-stop cycle journals `open` before `full_close` in the #1431 replay log; protection fields still backfill via the sync's re-stamp (#625/#669). **Round 13** close DMs label `Liquidation-clamp SL close` (`tradeAlertCloseSource` case); `reconcilePendingLimitOrders` counts a submit-fill SL close into its trade alert; the audit's protection-lost branch books under `liquidation_clamp_sl_immediate` (returns handled); `hlProtectionSLTriggerPx` delegates to `atrStopLossTriggerPx` (single formula owner) and capped audit re-arms log the #621 cap; and when NO strategy is due the loop runs an off-cycle audit pass on its own cadence — the fastest live-HL-perps interval via `liquidationAuditIntervalSeconds`, clamped to `tickSeconds`, fetch+maps shared with dispatch through `buildHLLiquidationMaps`, clock stamped on every run including fetch failure. **Round 14** the off-cycle branch `continue`s past the loop body's only `SaveStateWithDB`, so `runOffCycleLiquidationAudit` now RETURNS its booked-close count and `flushOffCycleLiquidationAuditState` persists inline before the sleep (the `reconcilePendingLimitOrders` shape) — the branch is re-entered on every quiet wake, so the unsaved window was a whole quiet period, not one tick; a failed flush latches `offCycleAuditSaveDirty` and retries on the next quiet wake, and counts into the shared `saveFailures` exactly like the end-of-cycle failure. Its exits post DIRECTLY to the operator channel — the per-cycle `channelTrades`/`channelTradeDetails` maps it used to write are re-created each iteration and the `continue` skips their consumer. `liquidationAuditIntervalSeconds` returns HALF the minimum interval (floored at `liquidationAuditMinIntervalSeconds`=60) over live HL perps **AND `manual`** — the bare minimum made the deadline elapse exactly when the strategy setting it became due (so the pass could never beat the dispatch audit), and excluding `manual` left a live HL manual-only fleet with no off-cycle pass at all though `collectHLLiquidationAuditCandidates` audits it. **Round-12 ordering needs an explicit post-arm stamp:** a PURE TRAILING owner never reaches `runHyperliquidProtectionSync`'s `stampOpenTradeWithProtectionSnapshot` backfill (`buildHyperliquidProtectionPlan` returns `syncOK=false` — `slMult` 0 and no on-chain tiers), so the early-recorded open row and the open DM's `SL:` line stayed blank for the position's life; `main.go` now stamps from the position after the #885 arm / scale-in re-size (`stampOpenTradeFromPosition` only fills still-zero fields, so an armed OID is never blanked). The same reorder also makes `recordPositionOpen` reachable on PAPER HL perps opens (previously live-only) — correct, and now pinned by a test. **Round 15** the off-cycle flush keys on `hlLiquidationAuditResult.StateMutations` (via `applyAuditStopUpdate`), NOT the booked-close count — a resting replacement / re-arm / dead-OID clear is the audit's NORMAL outcome and writes `StopLossOID`+`StopLossTriggerPx` without closing anything, so keying on closes left the new OID unsaved for the whole quiet period and a restart then chased a cancelled OID while the clamped order rested untracked. The dispatch-path `lastLiquidationAudit` stamp is now gated on `hlStateFetched` — the audit returns immediately without a snapshot, so stamping there recorded an audit that never happened and suppressed the off-cycle pass (which does its OWN fetch) for a full cadence; the off-cycle pass keeps its unconditional stamp (hot-retry guard). `run_sync_protection`'s `_place_sl` now RESOLVES an unreadable placement outcome by diffing `open_order_oids` against a pre-submit snapshot (exactly one new oid ⇒ adopt it as resting; none ⇒ genuine failure, clear as before), and only an unresolvable diff emits `stop_loss_outcome_unknown` — which makes `applyHyperliquidProtectionSync` KEEP `StopLossOID`/`StopLossTriggerPx` and swaps the false "position has NO exchange-side stop" CRITICAL for `hlProtectionStopOutcomeUnknown`'s verify-the-book alert. A positively rejected placement (`kind == "error"`) still clears and re-places. **Round 16** the WALKER/AUDIT sibling gets the same treatment: `applyTrailingStopUpdateResult`'s cancel-without-rest clear is now excluded on `StopLossOutcomeUnknown` (the clear made the position read `Unprotected`, which licensed a static-scalar audit re-arm and a trailing `currentTrigger<=0` re-arm to place a SECOND reduce-only stop whose OID was never recorded — uncancellable), and both classifiers route the shape to the new `hlLiquidationActionOutcomeUnknown` / `hlReplaceOutcomeUnknown` instead of `protection lost`. `hlLiquidationActionUnprotected` excludes it; the DM says the outcome could not be read and to verify the book. `hlLiquidationMayRetryReplace` is unchanged. The book-diff resolver is extracted to module-level `_resolve_sl_placement_by_book_diff` + `_snapshot_open_oids` and now ALSO runs in `run_update_stop_loss` — a fresh arm (`cancel_oid==0`) takes ONE pre-submit `open_order_oids` read so the re-arm paths can resolve too. The off-cycle audit clock stamps COMPLETION time, not the pre-run `now` — the branch continues without sleeping, so a pass lasting ≥ `audSec` re-entered immediately. **Round 17** placement classification is by what RESTS, never error text: Python emits `stop_loss_error` TOGETHER with a book-diff-resolved `stop_loss_oid` or `stop_loss_outcome_unknown`, so `hlLiquidationPlaceFresh` orders filled > resolved-OID > outcome-unknown > errors and both clamp retries adopt a resolved retry as a normal clamp; the audit's protection-lost state clear is CONDITIONAL on the in-cycle retry positively failing (an unreadable retry KEEPS recorded state); and the off-cycle pass is gated on the same `saveFailures >= 3` halt as dispatch (places nothing, books nothing; one FORCED probe save per cadence keeps recovery possible on a never-due fleet via `flushOffCycleLiquidationAuditState(..., force=true)`). Audit close alerts are grouped per strategy (`sendAuditCloseAlerts`) — per-detail calls re-emitted only the newest TradeHistory row. **Round 18** `hlLiquidationClampReplace` drops the cancel-flag gate from its outcome-unknown verdict — a FRESH placement (static-scalar re-arm, one-shot arm) never emits `cancel_stop_loss_succeeded`, so its unreadable outcomes fell to Deferred and re-armed duplicates every cycle; on that shape Go records the REQUESTED trigger with OID left 0 (`applyTrailingStopUpdateResult`, arm site), which closes every placement queue (audit reads it armed-at-a-reachable-trigger, `buildHyperliquidProtectionPlan` suppresses its SL leg when `StopLossOID==0 && StopLossTriggerPx>0`, the fixed-ATR arm site requires a clean slate) while `planHyperliquidLiquidationAudit` emits report-only `hlAuditReport` candidates so an unresolved shape keeps alerting instead of going silent; a positive rejection still clears and retries. `run_sync_protection` skips the TP tier walk when `stop_loss_filled_immediately`, and a placement resolved to a resting order emits NO `stop_loss_error` (a recorded OID plus an error field fired a false "protection failed" DM). **Round 19** outcome-unknown recording is UNIFIED across fresh placements AND tightens: requested trigger kept, OID left 0 — round 16's keep-the-old-OID left tightened state pointing at the just-cancelled order with its past-liquidation trigger, re-cancel+re-placing and stacking untracked reduce-only stops every unreadable cycle; on the {OID 0, reachable trigger} residue no queue re-places (audit report-only, protection-plan SL leg suppressed), a trailing owner heals via its own next min-move replace, and a new `placement unknown` alert action covers fresh arms/report-only candidates that cancelled nothing. The bankruptcy-bound preflight mirrors LoadConfig's `max_drawdown_pct` defaulting (platform-risk override → type default) instead of reading the raw field — omitted/zero printed OK for configs Go rejects at boot. Audit submit fills book the FILLED (`hlSLEffectiveQty`) quantity via `recordPerpsStopLossCloseQty`, leaving a cleared-protection residue for the reconciler. **Mechanics → ARCHITECTURE.md.**
- `hyperliquid_open_trailing.go` (#885) — arm initial SL inline at open. `hyperliquid_protection.go` — reduce-only TP/SL; on-chain TP = `strategyUsesTieredTPATRClose` AND live (paper never).
- `version_probe.go`/`probe_cmd.go`/`exit_codes.go` — new runtime CLI flag → both probe argvs; `ExitProbeFailure=78`.
- `trade_diagnostics*.go` (#1147) — EAGER insert in `recordClosedPosition`; MFE/MAE async outside `mu`; never write `llm_verdict`.
- `agent_info.go` (#1051) — read-only dump; `--bootstrap-md`→`AGENTS.generated.md` (NEVER `AGENTS.md`).
- `failure_alerts.go`/`script_failure_alerts.go` — wire notifier on new `run*Check`; primary at 3; **#1128** transient 429/5xx/timeout → WARN until 15 strikes or 75m.
- `discord_commands.go`/`discord_mutating_commands.go`/… — new mutating cmd → `opsCommandNames`+`slashCommands()`+dispatch; `/clear-cash-reconcile` (#1400); `/closing-strategies` read-only.
- `shared_wallet.go`/`shared_wallet_reconcile.go`/… (#918/#954/#921/#1408) — PRE-FEE `realized_pnl`; net via `tradeNetPnL*`; drift $0.01/2-cycle. **#1408 pool budgeting:** 2+ live HL/OKX perps omit capital fields + positive `margin_per_trade_usd` each; sizing from account equity − deployed margin; allocated↔pool flat-only/restart. **Mechanics → ARCHITECTURE.md.**
- `cashflow_journal.go`/… (#1100-#1106) — HL total-drift LIVE; OKX/TopStep shadow; OUTSIDE `mu`.
- `hl_reconcile_gap_alerts.go` (#971/#974) — alerting only.
- Also → ARCHITECTURE.md: `portfolio_warning.go`(#904)/`circuit_breaker_alert.go`(#905); `portfolio.go` (#1394/#1400 `CashReconcileRequired`); `kill_switch_close.go`+`*_close.go`; `missing_mark_alerts.go` (#1444, throttled owner DM keyed `(strategy_id, symbol)`); marks/deribit/init/sharpe/correlation/leaderboard/notifier/telegram/updater/pricer/tradingview_export/config_reload/backfill_hl_fees/hyperliquid_trailing_stop.

Other dirs (guardrails; inventories in ARCHITECTURE.md):
- `shared_scripts/` — per-platform `check_*.py` + `check_{strategy,options,price,regime}.py`, tuner/simulate. All accept `--regime-payload-json`; probed at startup. **#1442** `check_hyperliquid.py` splits the signal check into `build_shared_signal_state` (fetch/frame/ATR base/price + fetch memos) and `evaluate_signal_slot` (per-strategy, on `shared["df"].copy()`); single mode and `--batch-check` both run that pair — never fork a second implementation. Shared-state failure raises `SharedSignalStateError` → one `error_scope="shared_state"` sentinel; a slot exception stays in that slot.
- `platforms/<name>/adapter.py` — one `*ExchangeAdapter`/file. HL caches in `/tmp`; **#1128** lazy `_ensure_exchange`; sparse indices via `_normalize_spot_meta`.
- `shared_tools/` — `regime.py`; `funding_fetcher.py` (`merge_asof` backward; DISJOINT `funding_coverage`); ATR/RSI in `indicators_core.py` only; `atr_method` via `resolveATRMethod` (`regime.py` pinned `simple`).
- `shared_strategies/` — open SSoT `open/registry.py`; **`open/{spot,futures}/strategies.py` are shims — do not edit.** Close `close/registry.py`; options `options/strategies.py`.
- `backtest/` — `backtester.py`,`optimizer.py`,`run_backtest.py`,`backtest_{options,theta,pairs}.py`,`parity_diff.py`. See § Backtest.

## Key Patterns
- Run git from repo root. Prefer `go -C scheduler build .` over `cd scheduler &&`.
- **New platform (8):** adapter+`__init__`, `check_<name>.py`, `executor.go`, `config.go`, `fees.go`, `main.go` dispatch, `init.go`+`generateConfig`, `pyproject.toml`. Options also need vol/expiry/strike/premium helpers + `CalculateOptionFee` + `OptionPlatforms`.
- Adapters via `importlib`, class `endswith("ExchangeAdapter")`; check scripts use public methods only.
- **Close registry import:** `from close_registry_loader import evaluate, list_strategies, build_close_registry` — never bare `import registry`.
- Subprocess contract: JSON on stdout even on error; exit 1 on error; Go parses regardless of code.
- **State locking:** `mu sync.RWMutex`. 6-phase: RLock → Lock(CheckRisk) → no-lock(subprocess) → Lock(execute) → marks → RLock(status).
- Platform dispatch by `s.Platform` (never ID prefix). Prefix map `hl-`/`ibkr-`/`deribit-`/`ts-`/`rh-`/`okx-`/`luno-`, else BinanceUS.
- Types `spot`/`options`/`perps`/`futures`/`manual`. Perps paper→`ExecuteSpotSignalWithFillFee`, live→`RunHyperliquidExecute`; futures→`ExecuteFuturesSignalWithFillFee`. Manual auto-fills hold script; **#1115** close default regime→ratchet else `tiered_tp_atr_live`+SL@2×ATR.
- **Open/close split:** single `CloseStrategy` owns exit; close before open; partial-close preserves `InitialQuantity`, suppresses SL replace.
- **Live exec / skip-reason guards:** `liveExecFailed`; skip-reason checks BEFORE spawn; capture `posSide` with `posQty` in Phase 1 RLock.
- **Bidirectional perps:** short-entry → `bidirectionalPerpsStrategies`; flip sizing `perpsLiveOrderSize`. **→ ARCHITECTURE.md.**
- `dueStrategies` value-copied — update `cfg.Strategies` first. Notifications via `MultiNotifier`; channels `spot`/`options`/`<platform>`/`<platform>-paper`.
- HL: put `platforms/hyperliquid/` on sys.path before adapter import; funding via `meta_and_asset_ctxs()`.
- **Position ownership:** `OwnerStrategyID`; shared-coin reconcile non-destructive; SL attribution OID+qty else `hl_sync_external`.
- **State SQLite-only.** Trades: `is_close`/`realized_pnl`; `#T` counts opens by `(strategy_id, position_id)`; `HLFillLookup.ClosedPnLGross` is gross — never into `Trade.RealizedPnL`.
- **HL kill-switch fills:** shared-coin split by virtual qty; `hyperliquidKillSwitchFillShare` fails closed. Close-side: short→buy else sell.
- **Map iteration:** ALWAYS `sort.Strings(keys)` for operator/test output.
- **Regime:** `adx` default / `composite` opt-in. **#1124** bare `ranging_directional` covers `_up`/`_down` for gating (exact wins); certs stay exact-match.
- **Strategy registries:** `open/registry.py`+`PLATFORM_ORDER` + `knownShortNames` + `DEFAULT_PARAM_RANGES`. M5 `deprecated_m5` (32); live DM unless `allow_deprecated:true`; **#1402** paper auto-suppress via `AllowDeprecatedEffective()`. `backtest_only=True` fail-closes live. Snapshot `--list-json` before registry refactors.
- Strategy DSL: config params under runtime; `HTFFilter` not on options/`delta_neutral_funding`.
- **CB:** `#1048` disable suppresses new fires only; `#1273` tunable cooldowns; `#1046` latched HL-perps manage-only (`Signal=0`, not `continue`).
- **Kill switch:** `planKillSwitchClose`→`OnChainConfirmedFlat`. **#1368** `kill_switch_reset_dm_timeout` (empty→6h). **#1396** reset-prompt `atomic.Bool` single-flight.
- `initial_capital`: only `StateDB.SetInitialCapital`. **HL stops:** `EffectiveStopLossPct` seven mutual-exclusive owners; scalar↔regime blocked while open.
- **Risk-per-trade (#1268):** `risk_per_trade_pct` HL perps; fail-closed on unresolvable stop; exclusive vs sizing_leverage/margin/scale_in.
- **HL trailing SL:** cancel+replace > `TrailingStopMinMovePct`; `hlSLEffectiveQty=min(virtual,onChain)`; snapshot must carry full protection surface (#1015).
- **HL margin:** peers share `margin_mode`+`leverage`; `update_leverage` from flat only. On-chain TP suppression nils `CloseStrategy` for live tiered-TP (paper never).
- **SIGHUP:** `validateHotReloadCompatible` blocks add/remove, script/args/type/platform/HTFFilter, kill-switch identity, DB path, `max_notional_usd`. **Blocked-field list → ARCHITECTURE.md.**
- **New per-strategy flag:** field → `run*Check` CLI → Python parse → InitOptions/wizard. Runtime-required → both probe argvs.

## Pull Requests
- Reference issue with `Closes #<N>` in body. In GitHub comments avoid `#N` for list items; use `1.`.
- **PR title:** `type(#<N>): summary [C<score>, <model>, <effort>]` — Conventional Commits type (`feat`/`fix`/`refactor`/`chore`/`docs`/`ci`/`test`/`perf`/`style`), `#<N>` as scope when the PR closes an issue (else a short component name, or omit scope), lowercase imperative summary, then a trailing bracket with the issue's `[C<score>]` paired with the model/effort used to build the PR (append `, fableplan` when a Fable plan ran first). Example: `feat(#1340): add strategy tuning page [C40, GPT Sol, high]`. Standalone PRs (no issue) still use `type:` / `type(scope):` and may omit the bracket or derive score via the validate-issue formula.
- **PR body lead:** start with `## Plain simple English` — one short paragraph under 55 words, no jargon, no unexplained acronyms — stating what changed and why it matters, so a human can understand the PR without reading the technical summary. Then `## Summary` / verification; keep those scannable. Don't restate the whole issue.
- Fetch latest bot review: `gh api repos/richkuo/go-trader/issues/<N>/comments --jq '[.[] | select(.user.login=="claude[bot]" or .user.login=="github-actions[bot]")] | last | .body'` (top-level summary on issues endpoint, not pulls; #1178 review runs post as `github-actions[bot]`, older reviews as `claude[bot]`).
- Before merging a long-running PR: `git fetch origin main && git diff origin/main..HEAD -- <paths>` catches silent reverts.
- **Commits and PR bodies:** end with `LLM: <model> | <effort> | Harness: <action>` (default Claude Code footer slot). Do **not** append any `Co-authored-by:` / `Co-Authored-By` trailer.

### PR review format (`@claude review`)
**SSoT: `richkuo/rk-skills` `templates/claude-workflow/prompts/pr-review-format.md`**, fetched fresh each run. This repo keeps ONLY its delta in `.github/prompts/pr-review-format-local.md`, which `claude-run.yml` APPENDS to the shared body (read from `origin/main`, so an edit takes effect after merge). A full replacement at `.github/prompts/pr-review-format.md` would shadow the shared file and freeze it — do not recreate one. First line exactly `LGTM` or `Needs Updates` (blocking = `### Needs Fixing` / `### Requires Human Review` only). Safety findings never dropped. **Every finding** requires **Plain simple English:** (≤55 words, no jargon) after the technical description. Needs Fixing/Optional then require **Invariant:** + **Must survive:** (1–3 cases); Requires Human Review also requires **Recommended proposed solution:**. Prefer in-PR fixes; follow-up issue only when genuinely out-of-scope. **Reviews never gate on or wait for CI.** **Detail → ARCHITECTURE.md.**

### The Claude Code workflow itself (`.github/workflows/claude.yml` + the central run body)
- **#1178 least-privilege split:** `classify` + review/implement callers of `richkuo/rk-skills/.../claude-run.yml@main`; only call-site `permissions:` differ (review: no `id-token: write`). **Detail → ARCHITECTURE.md.**
- **Token model:** git/gh on Claude GitHub App token. Review comments as **`github-actions[bot]`**; implement as `claude[bot]`. Patch steps key on `RUN_ID`.
- **Mode routing fail-closed (#1334):** `pull_request_review*` → `review`; issues / non-PR comments / docs-sync/release → `implement`; else keyword after `@claude` (untrusted/fork → `review`; trusted → `fix-pr`).
- Comment patching: `patch_claude_comment.sh`+`compose_claude_comment.py`, staged from `rk-skills` `templates/claude-workflow/scripts/` into `$RUNNER_TEMP` each run — this repo carries no copy of them, and `.github/scripts/` holds ONLY `test_workflow_logic.py`, which extracts and executes the real `run:` blocks out of this repo's own `claude.yml`. Concurrency `group: claude-<N>`, `cancel-in-progress: false`.
- **No-execution ban** in workflow agent; commit/push implement-mode-only, exact-match. Prompt-file guard: the assembled prompt — shared body plus any `*-local.md` append — must not contain `"`, `` ` ``, or `$`.
- CLAUDE.md revision step must `git commit`+`git push origin HEAD`; immediately diff `HEAD -- CLAUDE.md` vs `origin/main` to catch silent reverts. `timeout-minutes: 90`. `issues: types: [opened]` only.

### Addressing review findings
Before implementing `@claude review` findings: restate each item as an invariant, enumerate states that would break the proposed fix (especially the inverse of the reported scenario and compound/concurrent cases), and add tests for that class — not only the reported example — before pushing.

## GitHub Issues
- Create with `gh issue create`; prefix the title with a complexity score: `[C<0-100>] <title>` (e.g. `[C70] Fix order-fill race`).
- **Complexity score (0–100):** an approximation, **not** a time/effort estimate. Weigh **scope** (files/layers/surfaces touched), **risk** (blast radius — money/data-integrity/security/auto-protective paths weigh heaviest), and **uncertainty** (unknowns/research before the work is well-defined).
- First body line is a one-line rationale naming the drivers, number matching the title: `**Complexity: 70/100** — scope: medium; risk: high (order-fill path); uncertainty: exchange API behavior unverified`.
- End the body with the same footer as commits/PRs — `LLM: <model> | <effort> | Harness: <action>` — no `Co-authored-by` trailer.

## Claude Code Skills (rk-skills plugin)
- The rk-skills workflow skills (`new-issue`, `validate-issue`, `work-on-issue`, `fix-pr-review`, `-loop` variants, `github-issue-format`, `pr-review-format`) are **CI-only**: `@claude` CI workflows load the plugin from [richkuo/rk-skills](https://github.com/richkuo/rk-skills) at run time (`plugin_marketplaces`/`plugins` in `richkuo/rk-skills/.github/workflows/claude-run.yml`), fetching the marketplace fresh each run.
- There is **no project-level settings pin** (no tracked `.claude/settings.json`; `.gitignore` ignores all of `.claude/*`). Interactive users who want the skills install them personally: `/plugin marketplace add richkuo/rk-skills` then install `rk-skills@rk-skills` via `/plugin`, or run `npx rk-skills`.

## Build & Deploy
- **Update:** `bash scripts/update.sh --restart` — atomic: preflight → `pull --ff-only` (or `--rsync-from <src>`) → `uv sync` → build → probe → swap (`.prev`) → restart+verify → rollback on timeout. **Never rebuild Go alone — argv contract requires both sides (Go+Python) at same SHA.** Batch `--all --restart` auto-discovers deployments via `discover_deployment_dirs_from_systemd`. **Detail → ARCHITECTURE.md.**
- **Graceful shutdown:** drains side-effecting subprocesses ≤ `shutdownDrainCap=15s` then SIGKILLs; state-save/notifier-flush/DB-close after via deferred LIFO. Unit `TimeoutStopSec=20`; service-file changes require `daemon-reload`.
- Build (one-off): `go build -ldflags "-X main.Version=$(git describe --tags --always --dirty=-mod)" -o go-trader .` (rebuild before smoke-testing). Restart `systemctl restart go-trader`; config-only `kill -HUP $(pgrep go-trader)`; Python picks up next cycle.
- **Startup probe:** binary invokes each unique check script with `--probe-only`; non-zero → log + owner DM + `os.Exit(ExitProbeFailure)` (exit 78, `EX_CONFIG`). `probeFailureScriptMissing` detects `"can't open file"` to distinguish missing-script deploy gaps from argparse mismatches. Both service files set `RestartPreventExitStatus=78`. Failures after update → ensure `shared_scripts/` updated + rebuild.
- **Post-update:** follow `SKILL.md` "Post-Update Agent Protocol" (diff `<running>..HEAD`, classify via Reference table, prompt per item). **Post-deploy smoke:** after a Python-launcher change, `./go-trader --config scheduler/config.json --once`.

## Backtest
**Mechanics → `docs/ARCHITECTURE.md` § Backtest harnesses; harness map → `docs/backtesting-registry.md` (update the row in the same PR that adds/deprecates a harness).**
- Run: `uv run --no-sync python backtest/run_backtest.py --strategy <n> --symbol BTC/USDT --timeframe 1h --mode single`; `backtest_options.py`/`backtest_theta.py --underlying BTC --since YYYY-MM-DD --capital 10000`; `backtest_pairs.py` (beta-hedged z-score, no live path).
- `--config <path> --strategy <id>` reads the single `close_strategy`, applies `user_defaults` by default; `--defaults system` preserves the built-in baseline. **#951: `--config` gates on `config_version>=15`.** Entry ATR guard 50%-of-AvgCost. **#1271** SL-vs-TP races default `ohlc_walk` (`--intrabar-resolution bar_close` = legacy).
- Regime: entries blocked when `bar_regime ∉ allowed_regimes`; closes always execute; options unsupported. **#1025** `--config` threads `allowed_regimes` (CLI flag rejected). **#1058** composite via `regime.windows`. **#1067** open name falls back to `args[0]`.
- **Backtester rejects (HL-live-only):** `regime_window_divergence`, `tiered_tp_atr_live_regime_dynamic`. `regime_directional_policy` backtestable behind **#1085**. Scalar `sl_after` + `*_atr_regime` dicts backtestable.
- **Look-ahead invariants:** signal at bar N fills at N+1 open; regime gate reads N-1 regime; closes use closed-bar ATR. **HTF series indexed by candle OPEN time MUST `.shift(1)` BEFORE `reindex(..., method="ffill")`.**
- **#1005 liquidation floor:** sticky equity floor at 0 from first bust; blown legs → `±LIQUIDATED_METRIC_FLOOR`.
- **M1–M6 / auto_suggest / regime promotion / `tune_live.py` (#1338/#1386 SCHEMA_VERSION=2 `promotion_baseline`):** SUGGEST-ONLY research surfaces — **never write live defaults/config/PRs.** **Detail → ARCHITECTURE.md.**

## Testing
- **New functionality must include tests.** Go `_test.go`; Python `test_*.py`; regression-test bug fixes. Extract pure helpers from subprocess wrappers — Go CI must not depend on spawning Python (`perpsLiveOrderSize`,`*OrderSkipReason`,`parseXxxCloseOutput`, Sharpe testable without subprocesses).
- `uv run --no-sync python -m py_compile <file>` from repo root. `go build -ldflags "…" .` / `go test ./...` from repo root (not `cd scheduler`); `gofmt -w <file>.go` post-edit. Multi-line Go edits with tabs may fail Edit tool; use Python heredoc with `read()`+`replace(old,new,1)`+`write()`.
- Strategy listing `…/open/{spot,futures}/strategies.py --list-json`; smoke `./go-trader --once`; JSON init `./go-trader init --json '{...}' --output /tmp/test.json`.
- Pytest `uv run --no-sync python -m pytest shared_strategies/ shared_tools/ platforms/ backtest/`; `shared_scripts/test_*.py` NOT in `testpaths` (invoke explicitly). **Registry/sys.path tests → run the FULL suite**; imports use `importlib.util.spec_from_file_location`. **#1304 CI runs pytest with `-n auto`** — never bare-`import` an ambiguous module name; treat an intermittent CI failure as test-isolation, never a flake. Go tests check `json.Unmarshal` errors. **→ ARCHITECTURE.md.**
- `stampEntryATRIfOpened` rejects ATR > 50% of AvgCost; Go tests using `atr` must use values < 50% of entry price. Strategy tests must assert actual signal values; smoke tests iterating registered strategies need `DatetimeIndex` (`amd_ifvg` reads `index.hour`, `vwap_reversion` buckets by `index.date`).
- **ATR for close evaluators:** `tiered_tp_atr`/`trailing_stop_atr_mult` need `Position.EntryATR`; `tiered_tp_atr_live` recomputes from `market_ctx["atr"]` (`atr_source` `live`|`entry`). `atr_stop` (#997) and `avwap_stop` (#1196) follow the same `atr_source` rule. `avwap_stop` is **virtual exit only** (not in `isTieredTPATRCloseName`/`closeStrategiesSuppressedByOnChainProtection`). **Detail → ARCHITECTURE.md.**
